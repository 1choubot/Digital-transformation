## 1. 数据模型与初始化

- [x] 1.1 新增数据库迁移，为 `users` 增加 `is_platform_admin` 字段，默认 `0`
- [x] 1.2 更新用户安全映射，返回 `name`、`isPlatformAdmin`，且不返回 `password_hash` 或密码内部字段
- [x] 1.3 更新初始化账号脚本，使初始化账号默认保存为 `isPlatformAdmin = true`
- [x] 1.4 确认禁用用户仍不能建立新登录态
- [x] 1.5 确认已有环境升级后，重新运行初始化账号脚本可把初始化账号设置/恢复为启用平台管理员
- [x] 1.6 统一用户姓名字段映射：请求 `displayName`，数据库 `display_name`，响应 `name`

## 2. 后端用户管理基础能力

- [x] 2.1 新增平台管理员校验中间件，要求 `requireAuth` 后当前用户 `isPlatformAdmin = true`
- [x] 2.2 新增用户管理仓储，支持用户列表、可选单用户查询、新增用户、编辑用户、启用/禁用和重置密码
- [x] 2.3 新增用户管理输入校验，覆盖必填字段、账号唯一、密码必填和安全字段过滤
- [x] 2.4 确保普通编辑接口不得修改 `account` 或 `password`
- [x] 2.5 确保用户管理接口不信任前端传入当前操作人
- [x] 2.6 确认用户管理不写登录审计、用户管理操作日志或项目业务操作日志
- [x] 2.7 增加“至少保留一个启用平台管理员”校验，覆盖禁用用户、取消 `isPlatformAdmin`、编辑 `isEnabled` / `isPlatformAdmin`
- [x] 2.8 对会导致 `0` 个启用平台管理员的操作返回稳定错误，并确保不保存本次修改

## 3. 后端用户管理接口

- [x] 3.1 新增用户列表接口
- [x] 3.2 如设计采用单用户查询，新增单用户查询接口并处理用户不存在错误
- [x] 3.3 新增用户接口，支持 `account`、`displayName`、`department`、`role`、`password`、`isEnabled`、`isPlatformAdmin`、`filePlatformUserId`
- [x] 3.4 新增编辑用户基础信息接口，支持 `displayName`、`department`、`role`、`isEnabled`、`isPlatformAdmin`、`filePlatformUserId`
- [x] 3.5 新增启用用户接口
- [x] 3.6 新增禁用用户接口
- [x] 3.7 新增重置用户密码接口
- [x] 3.8 确保所有用户管理接口要求平台管理员，不只做 `requireAuth`

## 4. 前端用户管理页面

- [x] 4.1 在前端当前用户状态中保存 `isPlatformAdmin`
- [x] 4.2 新增用户管理 API 客户端方法
- [x] 4.3 仅平台管理员展示用户管理入口；非管理员直接访问时展示无权限提示
- [x] 4.4 新增用户管理页面，展示用户列表和基础字段
- [x] 4.5 新增用户表单，使用 `displayName` 作为请求字段，并支持必填字段和可选 `filePlatformUserId`
- [x] 4.6 新增编辑用户基础信息表单，展示响应 `name`，提交 `displayName`，不允许普通编辑修改账号或密码
- [x] 4.7 新增启用/禁用用户操作，成功后刷新列表
- [x] 4.8 新增重置密码操作，使用单独交互提交新密码
- [x] 4.9 处理用户管理加载中、失败和空列表状态
- [x] 4.10 确认页面不新增项目成员、责任人、个人待办、复杂权限、文件平台同步、审计日志、单点登录或批量导入入口

## 5. README 文档

- [x] 5.1 更新 `digital-platform-api/README.md`，说明用户管理接口、平台管理员边界、字段、初始化账号管理员策略和不做范围
- [x] 5.2 更新 `digital-platform-web/README.md`，说明用户管理页面、管理员入口、可操作字段和不做范围

## 6. 验证

- [x] 6.1 在 `digital-platform-api` 下运行 `npm.cmd run check`
- [x] 6.2 在 `digital-platform-web` 下运行 `npm.cmd run build`
- [x] 6.3 验证未登录访问用户管理接口被拒绝
- [x] 6.4 验证非平台管理员访问用户管理接口被拒绝
- [x] 6.5 验证平台管理员可新增、编辑、启用、禁用和重置密码
- [x] 6.6 验证新增用户 `account` 唯一，缺少必填字段会被拒绝
- [x] 6.7 验证普通编辑接口不能修改 `account` 或 `password`
- [x] 6.8 验证禁用用户不能新登录
- [x] 6.9 验证用户管理响应不包含 `password_hash` 或密码内部字段
- [x] 6.10 验证非管理员前端不展示用户管理入口或直接访问时展示无权限提示
- [x] 6.11 验证用户管理不触发文件平台同步、不写用户管理操作日志
- [x] 6.12 验证初始化账号脚本默认创建/恢复启用平台管理员，且不会导致系统初始化后无人可管理
- [x] 6.13 验证禁用最后一个启用平台管理员、取消最后一个启用平台管理员或编辑导致无人管理时被拒绝
- [x] 6.14 验证用户管理请求使用 `displayName`，响应使用 `name`
- [x] 6.15 验证并发禁用或取消不同启用平台管理员时，不会导致 `0` 个启用平台管理员
- [x] 6.16 运行 `cmd /c openspec validate add-basic-user-management --strict`
