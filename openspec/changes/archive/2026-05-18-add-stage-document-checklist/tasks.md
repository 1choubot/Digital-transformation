## 1. 后端数据模型与模板数据

- [x] 1.1 新增阶段资料项模板表，字段覆盖阶段、资料编号、资料项名称、是否必填、默认责任部门或责任角色、确认角色、提交方式、`targetFolderPath`、可空 `targetFolderId`、模板版本
- [x] 1.2 新增项目级阶段资料项表，字段覆盖项目、阶段、资料编号、资料项名称、是否必填、责任字段、提交方式、`targetFolderPath`、可空 `targetFolderId`、基础状态和后续能力预留字段
- [x] 1.3 为项目级资料项增加项目、阶段、资料编号或资料 key 的唯一约束，保障补初始化幂等
- [x] 1.4 按 `docs/9.1_8阶段流程与阶段定义表.md` 和 `docs/9.2_阶段资料清单与责任角色表.md` 初始化第一版模板数据，资料项必须完全来自 `docs/9.2`
- [x] 1.5 将提交方式标准化为在线表单、文件上传、混合、暂未确定之一
- [x] 1.6 将资料项基础状态标准化为 `not_submitted`、`submitted`、`confirmed`、`returned`，初始化默认 `not_submitted`
- [x] 1.7 确认模板数据不添加 `docs/9.2_阶段资料清单与责任角色表.md` 之外的资料项
- [x] 1.8 如果实现时无法可靠解析 `docs/9.2_阶段资料清单与责任角色表.md`，暂停并说明，不自行编造资料项
- [x] 1.9 确认第一版只保存 `targetFolderPath`，`targetFolderId` 为空，待文件平台真实联动时再回填目录 ID

## 2. 后端初始化与补初始化

- [x] 2.1 实现项目级阶段资料清单初始化服务，按模板为项目生成资料项
- [x] 2.2 更新项目创建流程，在项目主数据和 8 阶段初始化后初始化阶段资料清单
- [x] 2.3 确认项目创建时资料清单初始化失败不会被当作完整成功
- [x] 2.4 实现历史项目补初始化后端脚本或命令，允许对已有项目补生成资料清单
- [x] 2.5 确认历史项目补初始化不会重复生成已存在资料项
- [x] 2.6 确认未补初始化的历史项目仍可读取项目列表和项目详情基础状态
- [x] 2.7 确认第一版不做前端补初始化按钮，不开放给普通用户调用的补初始化接口

## 3. 后端查询接口

- [x] 3.1 新增查询某项目阶段资料清单接口，例如 `GET /api/projects/:projectId/stage-document-checklist`
- [x] 3.2 接口按 8 阶段顺序分组返回资料项
- [x] 3.3 每个阶段分组返回阶段标识、阶段名称和资料项列表
- [x] 3.4 每个资料项返回资料编号、资料项名称、是否必填、默认责任部门或责任角色、提交方式、`targetFolderPath`、可空 `targetFolderId` 和基础状态
- [x] 3.5 处理项目不存在场景，返回项目不存在错误
- [x] 3.6 确认接口不返回资料齐套率、缺失统计或阶段推进判断

## 4. 前端项目详情展示

- [x] 4.1 扩展项目 API client，封装阶段资料清单查询接口
- [x] 4.2 在项目详情页新增“阶段资料清单”区域
- [x] 4.3 按阶段分组展示资料项基础信息和状态
- [x] 4.4 展示资料项名称、是否必填、默认责任部门或责任角色、提交方式、`targetFolderPath`、可空 `targetFolderId` 和基础状态
- [x] 4.5 处理资料清单加载中、接口失败和空清单状态
- [x] 4.6 将状态 `not_submitted`、`submitted`、`confirmed`、`returned` 分别展示为“待提交”、“已提交”、“已确认”、“已退回”
- [x] 4.7 确认项目详情页不提供上传、填写、提交、确认、退回、补初始化、阶段推进或齐套率计算按钮

## 5. 范围边界确认

- [x] 5.1 确认本变更不实现文件上传
- [x] 5.2 确认本变更不实现文件下载
- [x] 5.3 确认本变更不调用文件管理平台 API 或进行真实文件平台联动
- [x] 5.4 确认本变更不实现在线表单填写或表单生成归档文件
- [x] 5.5 确认本变更不实现资料齐套率计算
- [x] 5.6 确认本变更不实现阶段推进
- [x] 5.7 确认本变更不实现退回/确认操作、复杂权限或业务日志
- [x] 5.8 确认本变更不开放普通用户补初始化接口

## 6. 验证

- [x] 6.1 验证项目创建后自动初始化阶段资料清单
- [x] 6.2 验证历史项目可以补初始化资料清单
- [x] 6.3 验证补初始化重复执行不会重复生成资料项
- [x] 6.4 验证查询接口按阶段分组返回资料项
- [x] 6.5 验证资料项字段包含必填标记、责任字段、提交方式、`targetFolderPath`、可空 `targetFolderId` 和基础状态
- [x] 6.6 验证前端项目详情页能展示阶段资料清单基础信息和状态
- [x] 6.7 验证 `targetFolderId` 第一版为空，只保存 `targetFolderPath`
- [x] 6.8 验证前端将 `not_submitted` 显示为“待提交”
- [x] 6.9 验证项目详情页未提供上传、填写、提交、确认、退回、补初始化、阶段推进按钮
- [x] 6.10 验证未实现状态流转、文件上传/下载、在线表单填写、资料齐套率、阶段推进、确认/退回、复杂权限、普通用户补初始化接口或业务日志
- [x] 6.11 运行后端基础检查、前端构建或等价检查
- [x] 6.12 运行 `openspec validate add-stage-document-checklist --strict`
