## 1. 规划

- [x] 1.1 创建 `add-solution-design-quotation-online-form-v1` change。
- [x] 1.2 盘点现有报价/投标流程代码，确认 `quotation_file` 上传槽、节点激活、报价提交、报价结果和 C18 派生完成口径。
- [x] 1.3 读取 `报价单-模板.docx`，记录表格结构、可映射字段和已确认字段规则。
- [x] 1.4 编写 proposal，明确范围、Non-Goals、不归档、不提交、不 push。
- [x] 1.5 编写 design，包含现状盘点、模板字段映射、唯一当前口径、风险和 Non-Goals。
- [x] 1.6 编写 `project-core` 和 `technical-architecture` spec delta。
- [x] 1.7 运行 `cmd /c openspec validate add-solution-design-quotation-online-form-v1 --strict`。
- [x] 1.8 运行 `cmd /c openspec validate --all --strict`。
- [x] 1.9 运行 `git diff --check`。
- [x] 1.10 运行 `git status --short --branch`。

## 2. 后端实现

- [x] 2.1 设计报价单在线表单 schema、payload normalize 和 DTO。
- [x] 2.2 设计或新增报价单表单持久化结构，保留 revision、状态、提交人、提交时间、生成状态和失败原因。
- [x] 2.3 实现报价单 `.docx` 模板字段映射，包括 `TO`、明细行、总金额、大写金额、联系人、电话和日期。
- [x] 2.4 复用 `renderDocxTemplate()` 或现有 OOXML 渲染能力生成报价单 Word 文件。
- [x] 2.5 实现生成文件保存、失败清理、状态记录和下载。
- [x] 2.6 将报价节点门禁从新上传 `quotation_file` 改为当前 revision 在线表单生成文件成功。
- [x] 2.7 将 C18 派生完成从当前 revision `quotation_file` 上传改为当前 revision 报价单生成文件成功且报价被接受。
- [x] 2.8 实现人民币大写金额计算。
- [x] 2.9 实现报价明细动态行生成。
- [x] 2.10 确保旧 `quotation_file` 上传不再满足新报价流程提交、下载或 C18 完成门禁。
- [x] 2.11 报价分支选择后隐藏 `quotation_file` 上传入口，并在后端拒绝报价分支下新的 `quotation_file` 上传且返回明确业务错误。
- [x] 2.12 保持投标分支商务标/技术标上传路径不变。

## 3. 前端实现

- [x] 3.1 在报价分支下展示报价单在线表单入口。
- [x] 3.2 实现报价单明细行编辑、总金额展示、保存、提交和错误展示。
- [x] 3.3 展示生成状态、生成失败原因和 `.docx` 下载入口。
- [x] 3.4 保持投标分支 UI 不变。

## 4. 回归测试

- [x] 4.1 覆盖报价单在线表单保存和提交成功。
- [x] 4.2 覆盖提交后生成 `.docx`，并校验关键 Word 文本和表格内容。
- [x] 4.3 覆盖生成失败时不提交、不完成 C18、不允许处理报价结果。
- [x] 4.4 覆盖下载成功、storage key 为空和存储文件丢失错误。
- [x] 4.5 覆盖权限：总经理选择分支，商务负责人编辑/提交/处理结果，非授权用户被拒绝。
- [x] 4.6 覆盖报价被接受后 C18 完成、C19 不阻塞、自动推进仍触发。
- [x] 4.7 覆盖投标流程不回退。
- [x] 4.8 覆盖前端提交金额、总金额或大写金额被篡改时，后端重新计算或拒绝该输入。
- [x] 4.9 覆盖旧 `quotation_file` 上传不再满足新报价流程门禁。
- [x] 4.10 覆盖 10 行及以上报价明细，验证 Word 表格能动态扩行，并且总金额行、固定备注、公司名、日期仍在正确位置。
- [x] 4.11 覆盖数量、单价、每行金额、总金额和人民币大写金额的精度与舍入规则。
- [x] 4.12 覆盖报价分支选择后直接调用旧 `quotation_file` 上传接口被拒绝，且不得创建可用于 C18 的有效上传记录。
- [x] 4.13 覆盖报价单生成失败时提交接口返回明确业务错误，并保留草稿/生成失败状态。
- [x] 4.14 覆盖报价日期为空时基于 `submitted_at` Date 对象生成中文日期。
- [x] 4.15 覆盖草稿半填明细可保存、非法数字仍拒绝、提交时仍要求完整明细。
- [x] 4.16 修复 Word 明细单元格填充值时新增额外空段落的问题。
- [x] 4.17 修复报价单联系电话填充到 `电话` 标签后的下划线位置。
- [x] 4.18 前端报价明细改为原生表格控件体验。
- [x] 4.19 增加 Word 空段落、电话横线位置和报价表格相关回归覆盖。
- [x] 4.20 运行 `cmd /c npm.cmd run test:solution-design`。
- [x] 4.21 运行 `cmd /c npm.cmd run check`。
- [x] 4.22 运行 OpenSpec 校验和 `git diff --check`。

## 5. 收尾

- [x] 5.1 归档 change。
- [x] 5.2 提交实现。
