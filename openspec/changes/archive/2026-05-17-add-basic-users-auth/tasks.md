## 1. 后端数据模型与初始化

- [x] 1.1 新增数字化平台用户表迁移，包含账号、姓名、部门、角色、启用状态、可选 `filePlatformUserId` 和登录凭据字段
- [x] 1.2 为数字化平台用户账号增加唯一约束
- [x] 1.3 新增登录态或会话相关数据结构，用于根据前端登录态识别当前用户并支持退出登录
- [x] 1.4 为 `projects` 表新增可空 `createdByUserId` 字段，兼容现有历史项目和测试数据
- [x] 1.5 增加首批可登录用户初始化方式，支持至少一个启用用户用于第一版登录验证
- [x] 1.6 确认用户表、登录态数据和项目创建人字段只落在数字化平台 MySQL 中，不读取、不共用、不迁移文件管理平台数据库

## 2. 后端基础认证能力

- [x] 2.1 实现用户数据访问层，支持按账号查找用户、校验启用状态和返回安全用户对象
- [x] 2.2 实现密码哈希校验，登录响应不得返回密码、密码哈希或凭据内部字段
- [x] 2.3 实现 `POST /api/auth/login`，支持启用用户使用正确账号和密码登录
- [x] 2.4 实现 `POST /api/auth/logout`，支持主动结束当前登录态
- [x] 2.5 实现 `GET /api/auth/me`，根据有效登录态返回当前用户基础信息
- [x] 2.6 处理账号或密码错误场景，返回可读登录失败提示
- [x] 2.7 处理禁用用户登录场景，拒绝建立新的登录态
- [x] 2.8 处理未登录、登录态无效或登录态过期场景，提示需要重新登录
- [x] 2.9 确认基础认证接口不做文件平台用户同步、权限同步、下载权限判断或文件管理平台数据库访问

## 3. 后端项目创建人记录

- [x] 3.1 实现当前用户识别中间件或等价机制，供项目创建接口读取当前登录用户
- [x] 3.2 更新 `POST /api/projects`，要求携带有效登录态
- [x] 3.3 未登录、登录态无效或登录态过期创建项目时，后端拒绝创建并返回需要登录的错误
- [x] 3.4 项目创建成功时根据当前登录态写入 `createdByUserId`，不得信任前端提交的创建人字段
- [x] 3.5 更新项目列表查询，返回创建人基础信息或创建人字段，并兼容 `createdByUserId` 为空
- [x] 3.6 更新项目详情查询，返回创建人基础信息或创建人字段，并兼容 `createdByUserId` 为空
- [x] 3.7 确认创建人记录只用于认证和操作人追溯，不实现复杂菜单权限、文件下载权限、部门权限继承、审批流、SSO 或细粒度按钮权限

## 4. 前端认证与登录态

- [x] 4.1 新增统一认证 API client，封装 `POST /api/auth/login`、`POST /api/auth/logout` 和 `GET /api/auth/me`
- [x] 4.2 实现前端登录页，包含账号、密码输入和登录提交
- [x] 4.3 登录成功后保存登录态和当前用户，并进入项目核心页面
- [x] 4.4 退出登录时调用退出登录接口，清理本地登录态并返回登录页
- [x] 4.5 登录失败时展示账号或密码错误、用户禁用、登录态失效等可读提示
- [x] 4.6 应用启动时根据本地登录态调用当前用户接口恢复登录状态
- [x] 4.7 当前用户接口提示登录态失效时清理本地登录态并返回登录页

## 5. 前端项目页面接入

- [x] 5.1 在项目列表、新建项目和项目详情页面展示当前登录用户姓名、部门和角色
- [x] 5.2 新建项目请求必须携带登录态
- [x] 5.3 未登录或登录态失效创建项目时，前端展示需要登录的错误提示，且不得假装创建成功
- [x] 5.4 项目列表展示后端返回的创建人基础信息或创建人字段，并兼容创建人为空
- [x] 5.5 项目详情展示后端返回的创建人基础信息或创建人字段，并兼容创建人为空
- [x] 5.6 项目页面请求保留现有真实后端数据来源，不回退到 Demo 静态数据
- [x] 5.7 确认项目页面不引入复杂菜单权限、文件下载权限、部门权限继承、审批流、单点登录或细粒度按钮权限

## 6. 验证

- [x] 6.1 验证启用用户可使用正确账号和密码登录，并能获取当前用户信息
- [x] 6.2 验证 `POST /api/auth/logout` 后原登录态不能继续获取当前用户信息
- [x] 6.3 验证账号或密码错误时无法登录，并展示可读错误提示
- [x] 6.4 验证禁用用户无法登录
- [x] 6.5 验证登录响应和当前用户响应不包含密码、密码哈希或凭据内部字段
- [x] 6.6 验证刷新页面后前端能恢复登录态并在项目页面展示当前登录用户
- [x] 6.7 验证登录态失效后前端清理本地登录态并返回登录页
- [x] 6.8 验证已登录用户创建项目时后端记录 `createdByUserId`
- [x] 6.9 验证未登录创建项目时后端拒绝创建，并返回需要登录的错误
- [x] 6.10 验证项目列表和项目详情返回创建人基础信息或创建人字段
- [x] 6.11 验证历史项目允许 `createdByUserId` 为空，列表和详情仍可正常返回
- [x] 6.12 验证 `filePlatformUserId` 可为空，且本变更未读取、共用或迁移文件管理平台数据库
- [x] 6.13 验证未引入复杂菜单权限、文件下载权限、文件平台用户自动同步、权限同步、下载权限判断、部门权限继承、审批流、单点登录或细粒度按钮权限
- [x] 6.14 运行后端基础检查、前端构建或等价检查
- [x] 6.15 运行 `openspec validate add-basic-users-auth --strict`
