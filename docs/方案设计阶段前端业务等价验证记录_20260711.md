# 方案设计阶段前端业务等价验证记录（2026-07-11）

## 验证对象

- 项目：KRF0711。
- 范围：方案设计阶段 9 个独立节点页面，对照拆分前 `ProjectSolutionDesignWorkflowPanel.vue`。
- 原则：API、DTO、权限、revision、文件协议、状态机和自动推进不变。

## 静态等价矩阵

| 验收项 | 拆分前行为 | 独立页面行为 | 结论 | 修复位置 |
|---|---|---|---|---|
| 节点状态 | 状态、版本、激活/提交/通过时间、退回和阻塞原因 | 共享节点框架完整展示 | 通过 | `SolutionDesignNodeLayout.vue` |
| 普通文件 | revision、上传人/时间、保密、上传、下载、无权限提示 | 已恢复相同信息和 DTO 权限 | 通过 | `SolutionUploadSlots.vue` |
| 节点动作 | DTO 权限控制提交、审批、退回，pending 防重复 | 保持 DTO 权限和 pending | 通过 | `SolutionNodeActions.vue` |
| 准备节点 | 角色展示/分配、完整候选人及加载错误 | 已恢复 | 通过 | `SolutionPreparationPage.vue` |
| C05 表单 | 自动项目信息、全部业务字段、保存和提交 | 已恢复 | 通过 | `SolutionAnalysisPage.vue` |
| C05 图片 | 四组图片、数量/MIME/大小、下载/删除、生成失效 | 使用原 composable 校验并按图片权限展示操作 | 通过 | `SolutionAnalysisPage.vue`、`useSolutionAnalysisForm.js` |
| C05 生成门禁 | 未生成成功或不可下载时禁止提交节点 | 已恢复额外提交禁用条件 | 通过 | `SolutionAnalysisPage.vue` |
| C15/C16 | 全字段、可重复行、记录人 fallback、生成文件 | 换行编辑后由 composable 转数组，fallback 和生成详情保持 | 通过 | `SolutionReviewNodePage.vue`、`useSolutionReviewForm.js` |
| 三类成本 | 对应上传槽、提交、审批和退回 | 由当前节点 DTO 和上传槽直接驱动 | 通过 | `StandardSolutionNodePage.vue` |
| 报价/投标 | 分支、报价结果、投标专用提交、审批退回 | 已恢复 `canSubmitTender` 独立入口 | 通过 | `SolutionQuotationTenderPage.vue` |
| 统一刷新 | 成功触发一次刷新，下载/失败不刷新 | `business-state-changed` 单次上抛 | 通过 | `useSolutionDesignNodePage.js`、`useSolutionDesignWorkflow.js` |
| 路由 | 当前节点可通过项目节点路由定位 | 9 个页面均注册到 `NodePageRouter` | 通过 | `nodePages.js` |

## 自动验证结果

本轮实际结果：

- `digital-platform-web: npm.cmd run build`：通过，1681 modules transformed。
- `digital-platform-web: npm.cmd run check`：通过。
- `git diff --check`：通过。
- `digital-platform-api: npm.cmd run test:solution-design`：86/86 通过，0 fail。
- `rg`：生产节点页无旧 shell/总面板引用，无直接 API 调用和浏览器刷新。

### 第 6 步样式迁移复验

- 方案设计生产页面及共享组件的可见原生 `button/input/select/textarea`：0。
- `.primary-button/.ghost-button` 控件引用：0。
- 前端 `build/check`：通过，1690 modules transformed。
- 方案设计专项测试：86/86 通过。
- Element Plus 上传继续通过 `http-request` 转交现有 action；退回和图片删除取消确认时不发送请求。

### 第 7 步迁移清理

- 临时 `SolutionDesignNodePageShell.vue` 已删除。
- 旧 `ProjectSolutionDesignWorkflowPanel.vue` 及其方案设计 scoped CSS 已删除。
- 9 个独立节点页面继续由 `nodePages.js` 直接注册；全仓不再保留方案设计双实现。
- 全局 `.primary-button/.ghost-button/.state-panel/.stage-document-pill` 仍被其他业务页面使用，本阶段未跨范围删除。

## KRF0711 多角色浏览器回归

当前状态：**部分执行，尚不计为完整业务等价通过**。2026-07-11 已使用隔离无头 Chrome 对永久保留的专用回归项目 `SOLUTION_DESIGN_REGRESSION_20260711154721`（项目 ID 4479）执行六账号、九节点 URL 直达与刷新矩阵；54/54 通过，且页面未出现横向溢出。KRF0711 本身未修改。

| 节点/路径 | 角色 | 验收内容 | 状态 |
|---|---|---|---|
| 方案设计准备 | 研发中心经理、项目经理 | 角色分配、工作计划上传提交 | 待执行 |
| 项目方案分析 | 技术负责人、研发中心经理 | C05、图片、生成、提交、审批退回 | 待执行 |
| 方案设计 | 技术负责人 | 8 项产出、提交 | 待执行 |
| 内部/客户评审 | 技术负责人、研发中心经理 | C15/C16、生成、审批退回 | 待执行 |
| 三类成本 | 技术/采购/财务及对应经理 | 上传、提交、分级审批、退回 | 待执行 |
| 报价/投标 | 商务负责人、技术负责人、总经理 | 分支、报价结果、投标审批 | 待执行 |
| 全部 9 节点 | 六个指定测试账号 | URL 直达、刷新、标题、节点定位、登录态、横向溢出 | 54/54 通过；证据见 `.tmp/solution-design-regression/20260711154721/browser-matrix.json` |

响应式截图已在 1280、768、375px 三种宽度生成，均取方案设计准备节点。角色分配后的业务状态来自公开 API 合法推进：立项表单提交、协作填写和三级审批均未直接修改数据库。

尚未执行并不得记为通过：工作计划上传提交、C05/C15/C16 写入与生成文件、8 项设计产出、三类成本多级审批、报价接受、投标、退回研发成本及结束项目等会改变业务状态的浏览器操作链路。

## 当前结论

静态业务差异已完成修复，六账号九节点的基础浏览器矩阵已通过。只有上述业务写操作及三种互斥终态分支完成后，才能将第 5 步最终结论改为“业务等价验证通过”。后端完整 `check` 的既有模板生成问题独立记录，不纳入本次前端拆分结论。
