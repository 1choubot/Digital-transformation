## 1. 后端数据模型与迁移

- [x] 1.1 新增数据库迁移，为 `project_stage_documents` 增加 `is_applicable` 和适用性追溯字段
- [x] 1.2 更新阶段资料清单数据表初始化逻辑，保证新环境资料项默认适用
- [x] 1.3 确认现有资料项迁移后默认适用，原有 `status` 和状态追溯字段保持不变
- [x] 1.4 更新资料项映射逻辑，返回 `isApplicable`、`notApplicableByUserId`、`notApplicableAt`、`notApplicableReason`、`restoredApplicableByUserId` 和 `restoredApplicableAt`

## 2. 后端适用性操作接口

- [x] 2.1 新增标记不适用接口，要求登录态并记录不适用人、不适用时间和不适用原因
- [x] 2.2 新增恢复适用接口，要求登录态，清空当前不适用原因和不适用追溯字段，并记录恢复适用人和恢复适用时间
- [x] 2.3 为标记不适用操作增加非空不适用原因校验
- [x] 2.4 确认适用性操作接口校验资料项属于 URL 中的项目
- [x] 2.5 确认恢复适用不自动修改资料项原有 `status`
- [x] 2.6 确认适用性操作只使用 `requireAuth`，不实现复杂权限、角色权限或轻角色校验
- [x] 2.7 确认适用性操作不调用文件管理平台、不创建文件记录、不创建表单记录、不推进阶段、不写业务日志

## 3. 后端状态流转与齐套摘要

- [x] 3.1 更新资料项提交、确认、退回状态操作校验，拒绝已标记不适用的资料项
- [x] 3.2 确认不适用资料项状态操作失败时不改变 `status`、状态追溯字段或适用性字段
- [x] 3.3 调整阶段齐套摘要，只统计适用且必填的资料项
- [x] 3.4 确认不适用资料项不计入 `requiredTotal`、`confirmedRequiredCount` 或 `incompleteRequiredCount`
- [x] 3.5 确认不适用资料项不会出现在 `incompleteRequiredDocuments`
- [x] 3.6 确认建议资料仍不计入齐套摘要
- [x] 3.7 确认 `requiredTotal = 0` 时仍返回 `completionPercent = 100` 且缺失必填资料列表为空

## 4. 前端项目详情页

- [x] 4.1 扩展项目 API 客户端，封装标记不适用和恢复适用接口
- [x] 4.2 更新项目阶段资料清单数据适配，接收资料项适用性字段和适用性追溯字段
- [x] 4.3 在阶段资料清单中展示资料项“适用 / 不适用”
- [x] 4.4 展示不适用原因、标记不适用追溯字段和恢复适用追溯字段，允许追溯字段为空
- [x] 4.5 为适用资料项提供不适用原因输入和“标记不适用”操作
- [x] 4.6 前端校验不适用原因非空，未填写时不调用标记不适用接口
- [x] 4.7 为不适用资料项提供“恢复适用”操作
- [x] 4.8 确认不适用资料项不展示标记提交、确认或退回操作
- [x] 4.9 适用性操作成功后刷新该项目阶段资料清单，并同步刷新阶段齐套摘要和缺失必填资料列表
- [x] 4.10 添加文案说明“不适用”是人工业务判断，不代表资料已提交、已确认或已归档
- [x] 4.11 确认页面没有新增项目类型模板、自动批量标记不适用、文件上传/下载、在线表单、文件平台联动、阶段推进、管理层看板、业务日志、责任人分配、个人待办或权限配置入口

## 5. 验证

- [x] 5.1 验证新建或补初始化资料项默认适用
- [x] 5.2 验证标记不适用原因为空时前端和后端都拒绝操作
- [x] 5.3 验证标记不适用成功后返回并展示不适用原因和追溯字段
- [x] 5.4 验证已不适用资料项不能提交、确认或退回
- [x] 5.5 验证恢复适用成功后清空当前不适用原因和不适用追溯字段，并记录恢复适用追溯字段
- [x] 5.6 验证恢复适用后资料项原有 `status` 不被自动修改
- [x] 5.7 验证齐套摘要只统计适用且必填资料，不适用资料不进入缺失列表
- [x] 5.8 验证建议资料状态或适用性变化不影响 `completionPercent`
- [x] 5.9 验证所有必填资料均不适用或没有适用必填资料时，`requiredTotal = 0` 且 `completionPercent = 100`
- [x] 5.10 验证适用性操作成功后，项目详情页刷新清单并更新齐套摘要
- [x] 5.11 运行后端基础检查或等价测试
- [x] 5.12 运行前端构建或等价检查
- [x] 5.13 更新 `digital-platform-api/README.md`，说明适用性字段、适用性操作接口、齐套摘要新口径和能力边界
- [x] 5.14 更新 `digital-platform-web/README.md`，说明项目详情页不适用/恢复适用展示、操作和能力边界
- [x] 5.15 运行 `cmd /c openspec validate add-stage-document-applicability --strict`
