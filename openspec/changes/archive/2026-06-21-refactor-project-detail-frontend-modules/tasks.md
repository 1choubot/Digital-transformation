## 1. 梳理与拆分边界

- [x] 1.1 梳理 `ProjectDetailPage.vue` 当前模板区域、状态变量、computed、操作函数和刷新链路
- [x] 1.2 标记项目基础信息、阶段状态、阶段资料清单、资料项操作、阶段推进和业务日志的组件边界
- [x] 1.3 设计各组件 props/emits，确保写操作仍由页面容器协调既有 API 调用和刷新逻辑
- [x] 1.4 确认本 change 不修改后端接口、API 客户端路径、请求字段、响应字段、错误码或权限规则
- [x] 1.5 确认本 change 不新增文件上传、在线表单、文件平台联动、复杂权限、项目成员权限或业务日志能力

## 2. 项目详情页组件拆分

- [x] 2.1 保留 `ProjectDetailPage.vue` 作为路由级页面容器，负责加载项目详情、阶段资料清单、责任人候选用户和业务日志
- [x] 2.2 拆出项目基础信息/标题区域组件，保持项目编号、项目名称、客户、项目经理、参与部门、状态、计划时间、备注和创建人展示不变
- [x] 2.3 拆出阶段状态展示区域，保持 8 阶段状态、当前阶段突出展示和已完成项目当前阶段为空展示不变
- [x] 2.4 拆出阶段资料清单组件，保持按 8 阶段分组、资料项基础字段、状态、适用性、责任人和追溯字段展示不变
- [x] 2.5 在阶段资料清单组件中保留阶段齐套摘要和缺失适用必填资料列表展示
- [x] 2.6 拆出资料项状态操作子组件或局部组件，保留标记提交、确认、退回和退回原因输入行为
- [x] 2.7 拆出资料项适用性操作子组件或局部组件，保留标记不适用、恢复适用和不适用原因输入行为
- [x] 2.8 拆出资料责任人选择/展示组件或 helper，保留责任人候选用户、未分配、禁用责任人、分配和清空责任人行为
- [x] 2.9 拆出资料项追溯展示组件或 helper，保留提交、确认、退回、适用性和责任人变更追溯字段展示
- [x] 2.10 拆出阶段推进组件，保留当前阶段齐套门禁、缺失资料提示、推进操作和推进成功后刷新行为
- [x] 2.11 拆出业务日志组件，保留日志时间、操作人、动作类型或中文摘要、summary、加载中、失败和空状态展示
- [x] 2.12 保留现有边界说明文案，包括手工状态、人工适用性、资料责任人和阶段推进说明

## 3. Helper 与样式整理

- [x] 3.1 按最小范围抽取状态文案、资料操作可用性、责任人显示或追溯显示等纯前端 helper
- [x] 3.2 确认 helper 不包含后端请求、副作用、权限扩展或业务状态机变更
- [x] 3.3 迁移或补充必要样式，保持现有布局和可视层级基本不变，不做大范围样式重做
- [x] 3.4 确认拆分后没有引入新的 UI 框架或新的运行时依赖

## 4. 行为保持验证

- [x] 4.1 验证项目详情页仍可通过既有路由加载
- [x] 4.2 验证项目详情页仍调用 `GET /api/projects/:projectId`
- [x] 4.3 验证阶段资料清单仍携带登录态调用 `GET /api/projects/:projectId/stage-document-checklist`
- [x] 4.4 验证资料项标记提交、确认、退回和退回原因输入入口仍存在
- [x] 4.5 验证资料项标记不适用、恢复适用和不适用原因输入入口仍存在
- [x] 4.6 验证责任人候选用户加载、责任人展示、分配和清空责任人入口仍存在
- [x] 4.7 验证资料状态/适用性/责任人操作成功后仍刷新阶段资料清单、齐套摘要和必要业务日志
- [x] 4.8 验证阶段推进入口、当前阶段齐套门禁和缺失适用必填资料提示仍存在
- [x] 4.9 验证业务日志区域仍加载和展示，且保持只读
- [x] 4.10 验证项目不存在、清单加载失败、资料操作失败、阶段推进失败和业务日志失败时仍展示可读错误提示
- [x] 4.11 验证页面仍展示手工状态不代表文件上传或在线表单、齐套摘要基于人工适用性、责任人不代表权限控制等边界说明
- [x] 4.12 通过源码或轻量 smoke 确认拆分后的关键组件被项目详情页引用
- [x] 4.13 运行 `cmd /c npm.cmd run build`，确认前端 build 通过

## 5. 文档与 OpenSpec

- [x] 5.1 更新 `digital-platform-web/README.md`，说明项目详情页已模块化但业务能力和接口保持不变
- [x] 5.2 确认未修改后端业务代码、数据库迁移和后端 README
- [x] 5.3 确认未修改、完成或归档 `define-digital-platform-v1`
- [x] 5.4 运行 `cmd /c openspec validate refactor-project-detail-frontend-modules --strict`
- [x] 5.5 运行 `cmd /c openspec validate --all --strict`
- [x] 5.6 运行 `cmd /c openspec list --json`，确认 active changes 包含本 change 和 `define-digital-platform-v1`
