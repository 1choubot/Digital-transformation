## 1. 现状梳理

- [x] 1.1 梳理 `stageDocumentRepository.js` 当前导出函数、内部 helper、SQL 查询、事务入口和调用点
- [x] 1.2 梳理 `projectRepository.js` 当前导出函数、内部 helper、SQL 查询、事务入口和调用点
- [x] 1.3 梳理 `routes/projects.js` 当前路由注册顺序、中间件、参数解析 helper 和 handler 调用点
- [x] 1.4 梳理 `operationLogRepository.js`、附件仓储、用户候选、我的资料任务和项目总览对项目/阶段资料模块的依赖关系
- [x] 1.5 记录重构前 API 路径、请求字段、响应字段、错误码、HTTP 状态码和权限边界基线

## 2. 模块边界设计

- [x] 2.1 设计阶段资料模块目录/文件结构，覆盖清单查询/初始化、状态流转、适用性、齐套摘要、责任人和我的资料任务
- [x] 2.2 设计项目核心模块目录/文件结构，覆盖项目创建/列表/详情、阶段初始化/阶段查询、阶段推进和项目总览看板
- [x] 2.3 明确是否保留 `stageDocumentRepository.js` 和 `projectRepository.js` 作为兼容 re-export 或薄封装出口
- [x] 2.4 明确可抽取的纯 helper 范围，例如 ID/输入校验、映射函数、齐套摘要计算和阶段资料缺失列表构造
- [x] 2.5 明确不得抽取或合并的业务状态机边界，避免资料状态、适用性、责任人、附件和阶段推进规则相互污染

## 3. 阶段资料仓储拆分

- [x] 3.1 拆出阶段资料模板、项目级资料清单初始化和历史补初始化相关逻辑
- [x] 3.2 拆出阶段资料清单查询和资料项映射逻辑，保持返回字段和安全用户字段不变
- [x] 3.3 拆出阶段齐套摘要计算 helper，保持阶段清单、阶段推进门禁和项目总览复用口径一致
- [x] 3.4 拆出资料状态流转事务逻辑，保持状态机、追溯字段和业务日志同事务规则不变
- [x] 3.5 拆出资料适用性事务逻辑，保持原因校验、追溯字段、状态不自动改变和业务日志同事务规则不变
- [x] 3.6 拆出资料责任人分配/清空逻辑，保持候选口径、错误码、责任人追溯和业务日志同事务规则不变
- [x] 3.7 拆出我的资料任务查询逻辑，保持筛选、排序、返回字段和只读边界不变
- [x] 3.8 确认阶段资料附件仓储保持独立，必要 import 调整不改变附件上传、列表、下载、删除和日志规则

## 4. 项目核心仓储拆分

- [x] 4.1 拆出项目创建、项目列表、项目详情和项目基础映射逻辑
- [x] 4.2 拆出标准 8 阶段初始化、阶段查询和当前阶段选择逻辑
- [x] 4.3 拆出阶段推进门禁、阶段状态更新、项目完成和推进日志事务逻辑
- [x] 4.4 拆出项目总览看板筛选、汇总指标、项目卡片、当前阶段异常和齐套摘要聚合逻辑
- [x] 4.5 保持 `projectRepository.js` 既有 public export 兼容，或同步更新全部调用点并确认无遗漏
- [x] 4.6 确认项目核心拆分不新增数据库迁移、不修改表结构、不改变项目状态或阶段状态规则

## 5. 路由层整理

- [x] 5.1 整理 `routes/projects.js` 内部 handler/helper，使路由层只负责参数解析、中间件绑定和调用后端模块
- [x] 5.2 保持所有项目、阶段资料、责任人、附件、业务日志和阶段推进 API 路径不变
- [x] 5.3 保持 `requireAuth`、非平台管理员边界、未登录 401 和各接口权限规则不变
- [x] 5.4 确认 `/api/projects/overview-dashboard` 等静态路由仍注册在 `/:projectId` 动态路由之前
- [x] 5.5 确认附件接口 ID 校验优先级、文件参数校验和错误码不因路由整理改变

## 6. 文档与范围检查

- [x] 6.1 评估是否需要更新 `digital-platform-api/README.md` 的后端模块结构说明；如不需要，记录原因
- [x] 6.2 如更新 README，仅说明后端模块化结构，明确业务能力、接口、权限和用户可见行为保持不变
- [x] 6.3 确认不修改 `digital-platform-web` UI；如发生构建必需的最小 import 调整，说明原因并保持用户可见行为不变
- [x] 6.4 确认未新增运行时依赖、未新增数据库迁移、未修改 `define-digital-platform-v1`

## 7. 自动化验证

- [x] 7.1 后端运行 `cmd /c npm.cmd run check`
- [x] 7.2 前端运行 `cmd /c npm.cmd run build`
- [x] 7.3 运行 `cmd /c openspec validate refactor-backend-project-stage-document-modules --strict`
- [x] 7.4 运行 `cmd /c openspec validate --all --strict`
- [x] 7.5 运行 `cmd /c openspec list --json`，确认 active changes 包含本 change 和 `define-digital-platform-v1`

## 8. HTTP/MySQL Smoke

- [x] 8.1 Smoke：项目创建成功，项目列表和项目详情可读
- [x] 8.2 Smoke：项目创建后标准 8 阶段和项目级阶段资料清单初始化正确
- [x] 8.3 Smoke：阶段资料清单查询返回既有字段、责任人安全字段、适用性追溯字段和齐套摘要
- [x] 8.4 Smoke：资料提交、确认、退回状态流转成功路径和非法流转错误码保持不变
- [x] 8.5 Smoke：资料标记不适用、恢复适用成功路径和非法操作错误码保持不变
- [x] 8.6 Smoke：阶段齐套摘要和未完成适用必填资料列表口径保持不变
- [x] 8.7 Smoke：阶段推进门禁拒绝缺失适用必填资料，齐套后推进成功
- [x] 8.8 Smoke：业务日志查询返回项目创建、资料操作、适用性、责任人、附件和阶段推进日志
- [x] 8.9 Smoke：用户管理、责任人候选、责任人分配和清空不受影响
- [x] 8.10 Smoke：我的资料任务查询筛选、排序和只读边界保持不变
- [x] 8.11 Smoke：项目总览看板 summary、项目卡片、筛选、排序和只读边界保持不变
- [x] 8.12 Smoke：阶段资料附件上传、列表、下载、删除和失败路径保持不变
- [x] 8.13 Smoke：未登录接口仍按既有口径返回 401 或未登录错误
- [x] 8.14 Smoke：关键非法参数和错误码保持不变，包括项目 ID、资料项 ID、附件 ID、任务 status、项目总览 status/currentStageOrder

## 9. 源码回归检查

- [x] 9.1 检查拆分后不存在循环依赖或重复业务状态机实现
- [x] 9.2 检查业务日志写入仍处于对应业务事务内，失败操作不写日志
- [x] 9.3 检查阶段资料附件 multipart、文件名/MIME 校验、50MB 限制和单 `file` 字段策略不变
- [x] 9.4 检查没有新增文件管理平台、在线表单、消息提醒、复杂权限或批量操作入口
- [x] 9.5 检查 `routes/projects.js`、`stageDocumentRepository.js` 和 `projectRepository.js` 行数/职责已明显收敛，并记录拆分出的模块清单
