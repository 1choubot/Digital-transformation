# 数字化管理平台 测试步骤清单

> 编排原则：先按账号种类分层（5 种角色），再按功能模块细分。每个步骤标注预期行为，供手工回归与冒烟测试参考。
>
> 适用范围：以"代码中已经落地"的 12 个已完工模块为准，不包含未实现功能。

---

## 0. 测试环境与前置准备

### 0.1 测试账号矩阵

建议准备 6 个测试账号覆盖全角色画像：

| 账号 | 组织角色 | 部门 | isPlatformAdmin | 说明 |
|------|----------|------|-----------------|------|
| `admin` | `system_admin` | — | ✅ true | 系统管理员，用户管理唯一入口 |
| `gm_zhang` | `general_manager` | — | ❌ false | 总经理，全局业务权限 |
| `gma_li` | `general_manager_assistant` | — | ❌ false | 总经理助理，全局只读观察 |
| `cm_wang` | `center_manager` | `rd_center` | ❌ false | 研发中心负责人 |
| `cm_zhao` | `center_manager` | `manufacturing_center` | ❌ false | 制造中心负责人 |
| `emp_chen` | `employee` | `rd_center` | ❌ false | 研发中心员工 |

### 0.2 测试项目模板

| 项目名称 | 模式 | 项目经理 | 参与部门 | 说明 |
|----------|------|----------|----------|------|
| TEST-001 智能托盘产线 | `self_developed` | `cm_wang` | rd_center, manufacturing_center | 跨中心项目 |
| TEST-002 剥线机升级 | `self_developed` | `emp_chen` | rd_center | 单中心项目 |
| TEST-003 冷喷锌刷漆设备 | `outsourced` | `cm_zhao` | manufacturing_center | 外包项目 |

### 0.3 数据库状态

- [ ] `users` 表至少存在上述 6 个账号，且 `admin` 为 `is_enabled=1, is_platform_admin=1, organization_role=system_admin`
- [ ] `stage_document_templates` 已通过 `npm run init-stage-documents` 初始化 64 项资料模板
- [ ] `project_stage_documents` 已随项目创建而自动映射 64 条记录
- [ ] `sessions` 表无过期数据干扰

---

## 1. 系统管理员 (`system_admin` + `isPlatformAdmin`)

### 1.1 认证

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 1.1.1 | 使用 `admin` / 正确密码登录 | 登录成功，返回 `organizationRole=system_admin`、`isPlatformAdmin=true`，Token 有效 |
| 1.1.2 | 使用 `admin` / 错误密码登录 | 返回 401 `INVALID_CREDENTIALS` |
| 1.1.3 | 使用被禁用账号登录 | 返回 403 `USER_DISABLED` |
| 1.1.4 | 登录后调用 `POST /api/auth/logout` | 退出成功，Token 立即失效 |
| 1.1.5 | 退出后使用原 Token 调用 `GET /api/auth/me` | 返回 401 `UNAUTHENTICATED` |
| 1.1.6 | 登录后不操作，等待超过 `AUTH_SESSION_TTL_HOURS` 再访问 | 返回 401，Token 已过期 |
| 1.1.7 | 前端页面刷新后检查导航栏 | 显示"用户管理"入口（system_admin + isPlatformAdmin 判定） |

### 1.2 用户管理

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 1.2.1 | 进入用户管理页 | 展示全部用户列表（账号、姓名、部门、组织角色、岗位、启用状态、平台管理员标识） |
| 1.2.2 | 新增用户：填写 `account=test01`、`displayName=测试01`、`organizationRole=employee`、`department=rd_center`、`role=工程师`、密码 `Test@1234` | 创建成功，列表中可见新用户 |
| 1.2.3 | 新增用户：使用已存在的 `account` | 返回 409 `USER_ACCOUNT_EXISTS` |
| 1.2.4 | 新增用户：`organizationRole=system_admin` 但 `isPlatformAdmin=false` | 返回 400 `SYSTEM_ADMIN_PLATFORM_ADMIN_REQUIRED` |
| 1.2.5 | 新增用户：`organizationRole=employee` 但 `department` 不填 | 返回 400 `INVALID_DEPARTMENT` |
| 1.2.6 | 新增用户：`organizationRole=general_manager` 但填了 `department` | 返回 400 `INVALID_DEPARTMENT`（全局角色不应有部门） |
| 1.2.7 | 编辑用户 `test01`：修改姓名为"测试零一"、部门改为 `manufacturing_center` | 修改成功，列表刷新显示更新 |
| 1.2.8 | 禁用用户 `test01` | `isEnabled` 变为 `false`，该用户无法登录 |
| 1.2.9 | 启用用户 `test01` | `isEnabled` 变为 `true`，可重新登录 |
| 1.2.10 | 重置用户 `test01` 密码为 `NewPass@123` | 操作成功，旧密码失效，新密码可登录 |
| 1.2.11 | 尝试禁用自己（`admin`） | 返回 409 `LAST_ENABLED_SYSTEM_ADMIN_REQUIRED` |
| 1.2.12 | 尝试将 `admin` 的 `isPlatformAdmin` 改为 `false` | 返回 400 `SYSTEM_ADMIN_PLATFORM_ADMIN_REQUIRED` |
| 1.2.13 | 创建第二个系统管理员（`admin2`，`isPlatformAdmin=true`）后禁用 `admin` | 操作成功（存在两个启用平台管理员，保底约束满足） |
| 1.2.14 | 禁用 `admin2` 后再禁用 `admin` | 返回 409 `LAST_ENABLED_SYSTEM_ADMIN_REQUIRED` |

### 1.3 项目主数据

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 1.3.1 | 进入项目列表 | 项目列表可见（系统管理员可见所负责项目，不可见与自己无关的项目） |
| 1.3.2 | 点击"新建项目" | **入口不应可见**（系统管理员无创建项目权限） |
| 1.3.3 | 直接调用 `POST /api/projects` | 返回 403 `FORBIDDEN_OPERATION` |
| 1.3.4 | 进入某个有权限的项目详情（如系统管理员为项目经理的项目） | 可查看基础信息、阶段资料清单 |

### 1.4 阶段资料（仅限有权限的项目）

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 1.4.1 | 查看阶段资料清单（有权限的项目） | 按 8 阶段展示 64 项资料，含状态、负责人、提交方式 |
| 1.4.2 | 尝试提交资料 | 若系统管理员为项目经理：**允许提交**；否则**不允许**（入口隐藏/403） |
| 1.4.3 | 尝试确认资料 | **不允许**（系统管理员无审批权限） |
| 1.4.4 | 尝试退回资料 | **不允许** |
| 1.4.5 | 尝试标记资料为不适用 | **不允许**（canManageStageDocumentApplicability 不通过） |
| 1.4.6 | 尝试分配责任人 | **不允许**（canManageProjectResponsibility 显式排除 system_admin） |
| 1.4.7 | 上传/下载/删除附件 | 若为项目经理且有责任人权限：**允许**；否则不允许 |
| 1.4.8 | 尝试推进阶段 | **不允许**（canAdvanceProjectStage 显式排除 system_admin） |

### 1.5 阶段审批

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 1.5.1 | 提交阶段审批（有权限的项目） | 若系统管理员为项目经理：**允许**；否则不允许 |
| 1.5.2 | 通过/退回阶段审批 | **不允许**（审批权限限于总经理和本中心负责人） |
| 1.5.3 | 查看阶段审批历史 | 可查看（项目有权限即可） |

### 1.6 我的工作台

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 1.6.1 | 进入"我的资料任务"页 | 显示该账号负责的阶段资料任务（如有），可按状态和项目筛选 |
| 1.6.2 | 若系统管理员无数据：预期列表为空或仅显示作为责任人的任务 |

### 1.7 项目总览

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 1.7.1 | 进入项目总览页 | 展示跨项目汇总指标（总数/进行中/已完成/风险），**只含该账号有权限的项目** |
| 1.7.2 | 按项目状态筛选 | 筛选生效 |
| 1.7.3 | 按关键字筛选 | 筛选生效 |
| 1.7.4 | 点击项目行跳转详情 | 正确跳转 |
| 1.7.5 | 查看项目业务操作日志 | 展示该账号可见范围内的日志 |

### 1.8 日报模块

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 1.8.1 | 检查前端导航栏 | **不显示"写日报"入口**（employee 专用） |
| 1.8.2 | 直接调用 `GET /api/daily-reports` | 返回 403 `DAILY_REPORT_WRITER_REQUIRED` |
| 1.8.3 | 直接调用 `POST /api/daily-reports` | 返回 403 |

### 1.9 周报模块

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 1.9.1 | 检查前端导航栏 | **不显示"写周报"入口**（employee / center_manager 专用） |
| 1.9.2 | 直接调用 `POST /api/weekly-reports` | 返回 403 `WEEKLY_REPORT_WRITER_REQUIRED` |
| 1.9.3 | 进入周报汇总及考评页 | **不显示**（canReadWeeklyReviewOverview 排除 system_admin） |

### 1.10 中心日报汇总

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 1.10.1 | 进入中心日报汇总页 | 可查看全部中心的日报数据（`canReadAllCenters` 包含 system_admin） |
| 1.10.2 | 按中心 + 日期筛选 | 筛选生效，可跨中心 |
| 1.10.3 | 导出 Excel | 导出成功 |

---

## 2. 总经理 (`general_manager`)

### 2.1 认证

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 2.1.1 | 使用 `gm_zhang` / 正确密码登录 | 登录成功，`organizationRole=general_manager`，`isPlatformAdmin=false` |
| 2.1.2 | 退出登录 → 再次登录 | 生成新 Token，旧 Token 失效 |
| 2.1.3 | 前端刷新后检查导航栏 | **不显示"用户管理"入口**（非 system_admin） |
| 2.1.4 | 浏览器关闭后重开，`GET /api/auth/me` | Token 有效期内恢复用户信息 |

### 2.2 用户管理

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 2.2.1 | 检查前端导航栏 | **不显示"用户管理"入口** |
| 2.2.2 | 直接访问 `#/users` | 页面不应渲染（路由无匹配或后端拒绝） |
| 2.2.3 | 直接调用 `GET /api/users` | 返回 403 `PLATFORM_ADMIN_REQUIRED` |

### 2.3 项目主数据

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 2.3.1 | 进入项目列表 | **显示全部项目**（总经理全局可见） |
| 2.3.2 | 点击"新建项目" | 表单正常展示，创建项目 |
| 2.3.3 | 创建项目：填写完整信息后提交 | 创建成功，自动初始化 8 阶段和 64 项资料清单 |
| 2.3.4 | 创建项目：不填客户名称 | 返回 400，提示必填 |
| 2.3.5 | 创建项目：选择非启用用户为项目经理 | 返回 400 `PROJECT_MANAGER_USER_NOT_FOUND_OR_DISABLED` |
| 2.3.6 | 创建项目：项目名称为空 | 返回 400 |
| 2.3.7 | 进入任意项目详情 | 可查看完整信息：基础信息、8 阶段、资料清单、齐套摘要、业务日志 |
| 2.3.8 | 项目编号后置填写 `PUT /api/projects/:id/project-code` | 可填写非空唯一编号 |

### 2.4 阶段资料

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 2.4.1 | 查看阶段资料清单 | 全部 64 项可见，按阶段分组 |
| 2.4.2 | 提交资料 | **允许**（总经理全局可提交） |
| 2.4.3 | 确认资料 | **允许**（总经理全局可确认） |
| 2.4.4 | 退回资料并填写退回原因 | **允许**（总经理全局可退回），资料状态变为 `returned` |
| 2.4.5 | 退回后责任人重新提交（返工完成） | 状态流转 `returned` → `submitted`（需是责任人本人或总经理） |
| 2.4.6 | 标记资料为不适用 + 填写原因 | **允许**（总经理全局可标记），资料从齐套计算中排除 |
| 2.4.7 | 恢复已标记不适用的资料 | **允许**，资料重新参与齐套计算 |
| 2.4.8 | 分配资料责任人：从候选人列表选择 | **允许**（全局均可分配），分配后记录追溯 |
| 2.4.9 | 清空资料责任人 | **允许** |
| 2.4.10 | 上传附件（单文件 ≤ 50MB） | 上传成功，附件列表可见 |
| 2.4.11 | 上传附件（超 50MB） | 返回 413 或后端拒绝 |
| 2.4.12 | 下载附件 | 下载成功，文件名正确 |
| 2.4.13 | 删除附件（软删除） | 附件列表中消失，数据库记录 `deleted_at` |
| 2.4.14 | 下载已软删除的附件 | 返回 404 |

### 2.5 阶段审批与阶段推进

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 2.5.1 | 提交阶段审批 | **允许**（总经理全局可操作） |
| 2.5.2 | 通过阶段审批 | **允许**，状态变为 `approved` |
| 2.5.3 | 退回阶段审批 | **允许**，状态变为 `returned` |
| 2.5.4 | 阶段审批退回后重新提交 | **允许** |
| 2.5.5 | 查询阶段审批历史 | 展示完整审批记录（提交/通过/退回时间、操作人） |
| 2.5.6 | 当前阶段资料全部齐套（所有适用资料已确认）后推进阶段 | **允许**，项目 `current_stage` +1 |
| 2.5.7 | 当前阶段有未齐套资料时推进阶段 | 返回错误，提示"阶段资料未齐套" |
| 2.5.8 | 末阶段（第 8 阶段）推进完成 | 项目状态变为 `completed`，项目完成 |
| 2.5.9 | 立项审批多节点通过/退回 | 按节点逐项操作，含精准返工标记 |

### 2.6 我的工作台

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 2.6.1 | 进入"我的资料任务"页 | 显示分配的阶段资料任务（如有），按状态/项目筛选 |
| 2.6.2 | 总经理同时具有资料审核待办和阶段推进待办 | 待办列表正确展示 |

### 2.7 项目总览

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 2.7.1 | 进入项目总览 | 展示**全部项目**的汇总指标 |
| 2.7.2 | 齐套率计算 | 与项目详情中当前阶段齐套率一致 |
| 2.7.3 | "我的待办资料"指标 | 数值正确，点击跳转"我的资料任务"页 |
| 2.7.4 | 业务操作日志 | 可查看所有项目的操作日志 |

### 2.8 日报模块

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 2.8.1 | 检查前端导航栏 | **不显示"写日报"入口**（employee 专用） |
| 2.8.2 | 直接调用 `POST /api/daily-reports` | 返回 403 |

### 2.9 周报模块

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 2.9.1 | 检查前端导航栏 | **不显示"写周报"入口**（employee / center_manager 专用） |
| 2.9.2 | 进入"周报汇总及考评"页 | **可见**（`canReadWeeklyReviewOverview` 包含 general_manager） |
| 2.9.3 | 查看所有周报列表 | 展示全部中心员工和中心负责人的周报 |
| 2.9.4 | 评审中心负责人的周报：写评分和评语 | **允许**（`canReviewCenterManagerWeeklyReport`） |
| 2.9.5 | 评审中心负责人周报时检查日报明细对比 | 不展示日报对比表（`canReviewCenterManagerWeeklyReport` 不含日报对比） |
| 2.9.6 | 查看周报 AI/规则评分 | 展示评分详情 |
| 2.9.7 | 修改最终评分 | 保存成功，评审人记录正确 |
| 2.9.8 | 查看周报评审历史 | 展示完整评审记录 |
| 2.9.9 | 周报 Excel 导出 | 导出成功 |

### 2.10 中心日报汇总

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 2.10.1 | 进入中心日报汇总页 | 可选择查看全部中心的日报 |
| 2.10.2 | 切换中心查询 | 可跨中心查看 |
| 2.10.3 | 导出 Excel（全部中心） | 导出成功，含全中心数据 |
| 2.10.4 | 周休息模式配置（单休/双休） | **允许**（`canManageWeeklyRestMode`） |

---

## 3. 总经理助理 (`general_manager_assistant`)

### 3.1 认证

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 3.1.1 | 使用 `gma_li` 登录 | 登录成功，`organizationRole=general_manager_assistant` |
| 3.1.2 | 前端刷新检查导航栏 | **不显示"用户管理"**、**不显示"写日报"**、**不显示"写周报"** |

### 3.2 项目主数据

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 3.2.1 | 进入项目列表 | **显示全部项目**（全局可见，只读） |
| 3.2.2 | 点击"新建项目" | **入口不可见**（助理无创建权限） |
| 3.2.3 | 进入项目详情 | 可查看基础信息和阶段资料清单 |

### 3.3 阶段资料

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 3.3.1 | 提交资料 | **不允许**（canSubmitStageDocument：只有总经理/项目经理/责任人/本中心负责人） |
| 3.3.2 | 确认/退回资料 | **不允许**（canApproveStageDocument：只有总经理/本中心负责人） |
| 3.3.3 | 标记不适用/恢复适用 | **不允许**（canManageStageDocumentApplicability：只有总经理/本中心负责人） |
| 3.3.4 | 分配责任人 | **不允许**（canManageProjectResponsibility 显式排除 assistant） |
| 3.3.5 | 推进阶段 | **不允许**（canAdvanceProjectStage 显式排除 assistant） |

### 3.4 周报模块

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 3.4.1 | 进入"周报汇总及考评"页 | **可见**（`canReadWeeklyReviewOverview` 包含 general_manager_assistant） |
| 3.4.2 | 查看中心负责人周报 | **允许查看**（`canReadCenterManagerWeeklyReport`），但**不能评分** |
| 3.4.3 | 查看周报列表 | 展示全部员工和中心负责人周报，只读 |

### 3.5 中心日报汇总

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 3.5.1 | 进入中心日报汇总 | **可见**，可查看全部中心日报 |

---

## 4. 中心负责人 (`center_manager`，以 `rd_center` 为例)

### 4.1 认证

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 4.1.1 | 使用 `cm_wang` 登录 | 登录成功，`organizationRole=center_manager`、`department=rd_center` |

### 4.2 用户管理

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 4.2.1 | 检查前端导航栏 | **不显示"用户管理"入口** |
| 4.2.2 | 直接调用 `GET /api/users` | 返回 403 |

### 4.3 项目主数据

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 4.3.1 | 进入项目列表 | **仅显示本中心（rd_center）相关的项目**（TEST-001、TEST-002），不显示 TEST-003 |
| 4.3.2 | 点击"新建项目" | **允许创建** |
| 4.3.3 | 创建项目：选择参与部门为 `manufacturing_center` | 创建成功，但该项目若未包含 rd_center，则中心负责人创建后无法查看（自身的部门不匹配） |
| 4.3.4 | 进入本中心关联项目的详情 | 正常展示 |
| 4.3.5 | 尝试进入非本中心项目（直接 URL） | 返回 403 或加载失败 |

### 4.4 阶段资料（仅限本中心相关）

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 4.4.1 | 提交资料（本中心相关） | **允许** |
| 4.4.2 | 确认资料（本中心相关） | **允许** |
| 4.4.3 | 退回资料（本中心相关） | **允许**，填写退回原因 |
| 4.4.4 | 尝试确认/退回非本中心相关的资料项 | 返回 403 |
| 4.4.5 | 标记不适用（本中心相关） | **允许** |
| 4.4.6 | 恢复适用（本中心相关） | **允许** |
| 4.4.7 | 分配责任人：从候选人列表选本部门员工 | **允许**（只能分配本部门用户） |
| 4.4.8 | 分配责任人：尝试选其他中心员工 | 返回 403 或无此选项 |
| 4.4.9 | 清空责任人（本中心相关） | **允许** |
| 4.4.10 | 附件上传/下载/删除（本中心相关） | **允许** |

### 4.5 阶段审批与阶段推进

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 4.5.1 | 提交阶段审批（本中心项目） | **允许** |
| 4.5.2 | 通过阶段审批（本中心项目） | **允许** |
| 4.5.3 | 退回阶段审批（本中心项目） | **允许** |
| 4.5.4 | 推进阶段（本中心关联项目、资料齐套） | **允许**，阶段+1 |
| 4.5.5 | 推进阶段（非本中心项目） | 返回 403 |

### 4.6 我的工作台

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 4.6.1 | 进入"我的资料任务"页 | 显示已分配的资料任务（本中心范围内） |
| 4.6.2 | 资料审核待办 | 显示本中心适用的 `approval_required` 资料审核项 |

### 4.7 项目总览

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 4.7.1 | 进入项目总览 | **仅展示本中心相关项目**的汇总指标 |

### 4.8 日报模块

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 4.8.1 | 检查前端导航栏 | **不显示"写日报"入口**（employee 专用） |
| 4.8.2 | 直接调用 `POST /api/daily-reports` | 返回 403 |

### 4.9 周报模块

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 4.9.1 | 检查前端导航栏 | **显示"写周报"入口** |
| 4.9.2 | 写周报：填写总结、下周计划 | **允许**（`canWriteWeeklyReport` 包含 center_manager） |
| 4.9.3 | 进入"我的周报列表" | 展示历史周报，可编辑/删除 |
| 4.9.4 | 进入"周报汇总及考评" | **可见**，但**仅显示本中心员工**周报 |
| 4.9.5 | 评审本中心员工的周报：填写评分和评语 | **允许**（`canReviewEmployeeWeeklyReport`：同中心+员工角色） |
| 4.9.6 | 评审时查看日报对比表 | 展示该员工本周日报与周报的明细对比 |
| 4.9.7 | 评审非本中心员工的周报 | 不应显示或返回 403 |
| 4.9.8 | 评审另一个中心负责人的周报 | 不应显示或返回 403 |
| 4.9.9 | AI/规则自动评分 | 系统触发，展示评分详情与评语模板 |
| 4.9.10 | 修改最终评分和评语 | **允许**，保存后记录评审人 |
| 4.9.11 | 周报 Excel 导出 | **仅导出本中心范围** |

### 4.10 中心日报汇总

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 4.10.1 | 进入中心日报汇总 | **仅显示本中心（rd_center）**的日报 |
| 4.10.2 | 尝试切换到其他中心 | 不应显示或返回 403（`assertSameDepartmentOrAllCenters`） |
| 4.10.3 | 导出 Excel | **仅导出本中心数据** |

---

## 5. 员工 (`employee`，以 `rd_center` 为例)

### 5.1 认证

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 5.1.1 | 使用 `emp_chen` 登录 | 登录成功，`organizationRole=employee`、`department=rd_center` |
| 5.1.2 | 被禁用后尝试登录 | 返回 403 `USER_DISABLED` |

### 5.2 用户管理

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 5.2.1 | 检查前端导航栏 | **不显示"用户管理"入口** |

### 5.3 项目主数据

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 5.3.1 | 进入项目列表 | **仅显示本中心相关且有权限的项目**（自己为项目经理、资料责任人，或参与的部门项目） |
| 5.3.2 | 点击"新建项目" | **入口不可见**（员工无创建权限） |
| 5.3.3 | 进入有权限的项目详情 | 可查看基础信息和阶段资料清单 |

### 5.4 阶段资料（仅限本中心相关 + 是责任人）

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 5.4.1 | 提交自己负责的资料 | **允许** |
| 5.4.2 | 尝试提交他人负责的资料 | 返回 403 或按钮不可点击 |
| 5.4.3 | 确认资料 | **不允许** |
| 5.4.4 | 退回资料 | **不允许** |
| 5.4.5 | 标记不适用 | **不允许** |
| 5.4.6 | 分配责任人 | **不允许** |
| 5.4.7 | 上传附件（自己负责的资料） | **允许** |
| 5.4.8 | 下载附件 | **允许** |
| 5.4.9 | 删除附件（只有上传者或审批者可删除） | 按附件权限判断 |

### 5.5 阶段审批与阶段推进

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 5.5.1 | 提交阶段审批 | 若为项目经理：**允许**；否则**不允许** |
| 5.5.2 | 通过/退回阶段审批 | **不允许** |
| 5.5.3 | 推进阶段 | 若为项目经理：**允许**；否则**不允许** |
| 5.5.4 | 查看阶段审批历史 | 可见（有项目权限即可） |

### 5.6 我的工作台

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 5.6.1 | 进入"我的资料任务" | 显示该员工负责的所有阶段资料任务 |
| 5.6.2 | 按状态筛选 | 待提交/已提交/已退回/已确认 分别筛选正确 |
| 5.6.3 | 按项目筛选 | 筛选后仅显示该项目资料 |
| 5.6.4 | 点击任务跳转项目详情 | 正确跳转到对应项目的详情页，定位到该阶段 |
| 5.6.5 | 被退回的任务 | 展示退回原因，状态为 `returned` |
| 5.6.6 | 被分配新任务 | 页面刷新后列表即时更新 |

### 5.7 项目总览

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 5.7.1 | 进入项目总览 | **仅展示本中心相关项目的汇总** |
| 5.7.2 | "我的待办资料"指标 | 与"我的资料任务"页数量一致，可点击跳转 |

### 5.8 日报模块

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 5.8.1 | 检查前端导航栏 | **显示"写日报"入口** |
| 5.8.2 | 写日报：选择日期、关联项目（可多选搜索）、填写事项 | 保存成功 |
| 5.8.3 | 写日报：不选任何项目 | 允许（项目关联非必填） |
| 5.8.4 | 写日报：事项列表为空 | 后端校验，酌情返回 400 或允许空内容 |
| 5.8.5 | 编辑已有日报 | 修改后保存成功 |
| 5.8.6 | 删除日报 | 删除成功，列表不再显示 |
| 5.8.7 | 删除他人日报 | 返回 403（员工只能删除自己的日报） |
| 5.8.8 | "我的日报列表"页 | 按日期降序展示，可筛选 |
| 5.8.9 | 日报附件：上传 | 上传成功，列表可见 |
| 5.8.10 | 日报附件：下载 | 下载成功 |
| 5.8.11 | 日报附件：删除 | 删除成功 |
| 5.8.12 | 日报 Excel 导出 | 导出成功，文件包含日报详情 |
| 5.8.13 | 跨天重复保存同一天日报 | 同一天只允许一条日报，第二次保存应报错或覆盖（取决于业务规则） |

### 5.9 周报模块

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 5.9.1 | 检查前端导航栏 | **显示"写周报"入口** |
| 5.9.2 | 写周报：自动绑定当前自然周 | 周起始/结束日期正确（取决于单休/双休模式） |
| 5.9.3 | 写周报：填写总结、下周计划 | 保存成功 |
| 5.9.4 | 编辑已有周报 | 修改后保存成功 |
| 5.9.5 | 删除周报 | 删除成功 |
| 5.9.6 | "我的周报列表"页 | 按周降序展示 |
| 5.9.7 | 周报附件：上传/下载/删除 | 操作正常 |
| 5.9.8 | 周报 Excel 导出 | 导出成功 |
| 5.9.9 | 进入"周报详情/评审"页 | 查看自己的周报详情，**不可自行评分** |
| 5.9.10 | 进入"周报汇总及考评" | **入口不可见**（员工无汇总权限） |

### 5.10 中心日报汇总

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 5.10.1 | 检查前端导航栏 | **不显示"中心日报汇总"入口** |
| 5.10.2 | 直接调用 `GET /api/center-daily-reports` | 返回 403 |

---

## 6. 跨角色联动测试

### 6.1 资料责任人变更通知

| 序号 | 前置操作 | 测试步骤 | 预期结果 |
|------|----------|----------|------|
| 6.1.1 | 总经理将资料 X 责任人从 `emp_chen` 改为 `cm_wang` | `cm_wang` 登录后进入"我的资料任务" | 任务列表中出现资料 X |
| 6.1.2 | 同上 | `emp_chen` 登录后进入"我的资料任务" | 资料 X 从列表中消失 |

### 6.2 资料退回重提闭环

| 序号 | 前置操作 | 测试步骤 | 预期结果 |
|------|----------|----------|------|
| 6.2.1 | 员工 `emp_chen` 提交资料 Y | 中心负责人 `cm_wang` 退回资料 Y，填写原因"缺少签字" | 资料 Y 状态变为 `returned` |
| 6.2.2 | 同上 | 员工 `emp_chen` 登录，看到资料 Y 被退回及原因 | 可见退回原因，可重新提交 |
| 6.2.3 | 同上 | 员工 `emp_chen` 修改后重新提交 | 资料 Y 状态变为 `submitted` |
| 6.2.4 | 同上 | 中心负责人 `cm_wang` 确认资料 Y | 资料 Y 状态变为 `confirmed` |

### 6.3 阶段推进权限

| 序号 | 前置操作 | 测试步骤 | 预期结果 |
|------|----------|----------|------|
| 6.3.1 | TEST-001 第 1 阶段资料全部确认齐套 | 总经理 `gm_zhang` 推进阶段 | **允许**，阶段推进到第 2 阶段 |
| 6.3.2 | 同上 | 中心负责人 `cm_wang` 推进阶段 | **允许**（rd_center 参与 TEST-001） |
| 6.3.3 | 同上 | 中心负责人 `cm_zhao` 推进阶段 | **允许**（manufacturing_center 参与 TEST-001） |
| 6.3.4 | 同上 | 员工 `emp_chen` 推进阶段（非项目经理） | 返回 403 |
| 6.3.5 | 同上 | 总经理助理 `gma_li` 推进阶段 | 返回 403 |
| 6.3.6 | 同上 | 系统管理员 `admin` 推进阶段 | 返回 403 |

### 6.4 多节点立项审批

| 序号 | 前置操作 | 测试步骤 | 预期结果 |
|------|----------|----------|------|
| 6.4.1 | 立项审批配置多个节点 | 总经理逐个节点通过 | 每节点通过后记录审批历史 |
| 6.4.2 | 某节点退回 | 相关资料标记精准返工 | 责任人只能看到被退回标记的资料 |
| 6.4.3 | 责任人返工完成后重新提交 | 状态恢复，审批人可见重新提交项 |

### 6.5 周报评审闭环

| 序号 | 前置操作 | 测试步骤 | 预期结果 |
|------|----------|----------|------|
| 6.5.1 | `emp_chen` 提交周报 | `cm_wang` 进入汇总页评分（含 AI 评分参考） | 可查看日报对比、AI 评分，可写最终评分和评语 |
| 6.5.2 | 同上 | `emp_chen` 查看自己周报评审结果 | 可见最终评分、等级、评语、评审人 |
| 6.5.3 | `cm_wang` 提交周报 | `gm_zhang` 进入汇总页评分 | 可评分（不展示日报对比） |

---

## 7. 边界与异常测试

### 7.1 登录安全

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 7.1.1 | 短时间内大量错误密码尝试 | 不应锁死或 500（取决于是否有限流） |
| 7.1.2 | 密码为空 | 返回 400 参数校验失败 |
| 7.1.3 | 账号为空 | 返回 400 参数校验失败 |
| 7.1.4 | 跨设备同时登录同一账号 | 各自生成独立 Token，均可正常使用 |
| 7.1.5 | Token 篡改后访问 | 返回 401 |

### 7.2 附件操作

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 7.2.1 | 上传空文件 | 返回 400 或后端拒绝 |
| 7.2.2 | 上传非预期格式（如 .exe） | 按附件类型校验策略处理 |
| 7.2.3 | 并发上传同一资料多个附件 | 均上传成功 |
| 7.2.4 | 物理文件被手动删除后下载 | 返回 500 `ATTACHMENT_FILE_MISSING` |

### 7.3 数据一致性

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 7.3.1 | 资料提交 + 业务日志写入原子性 | 日志写入失败时，资料提交操作回滚 |
| 7.3.2 | 同时推进同一项目阶段（并发） | 数据库锁保证只有一次推进成功 |
| 7.3.3 | 前端数据与后端 API 响应一致 | 刷新页面后，前端状态与 `GET` 接口返回一致 |

### 7.4 业务日志完整性

| 序号 | 测试步骤 | 预期结果 |
|------|----------|----------|
| 7.4.1 | 项目创建 | 日志记录"用户 X 创建项目 Y" |
| 7.4.2 | 资料提交 | 日志记录"用户 X 提交资料 Y" |
| 7.4.3 | 资料确认 | 日志记录"用户 X 确认资料 Y" |
| 7.4.4 | 资料退回 | 日志记录"用户 X 退回资料 Y，原因：..." |
| 7.4.5 | 责任人变更 | 日志记录"用户 X 将资料 Y 责任人从 A 变更为 B" |
| 7.4.6 | 附件上传 | 日志记录"用户 X 上传附件 Z 至资料 Y" |
| 7.4.7 | 阶段推进 | 日志记录"用户 X 推进项目 Y 从阶段 S 至阶段 S+1" |
| 7.4.8 | 项目完成 | 日志记录"用户 X 完成项目 Y" |

---

## 附录 A：测试执行建议

1. **冒烟测试**（每次发版前）：执行 5 个角色各 2-3 条核心路径即可
2. **全量回归**：建议按角色顺序依次执行，每人次约 30-45 分钟
3. **权限回归**：重点覆盖"不该有的操作被拒绝"（403 校验），不应仅测试正向路径
4. **数据准备**：建议用独立测试库，测试前执行 `reset-stage-documents-v20260625` 脚本重置资料数据

## 附录 B：错误码速查

| HTTP 状态 | 常见错误码 | 触发场景 |
|-----------|-----------|----------|
| 400 | `INVALID_PARAMS` | 参数缺失或格式错误 |
| 400 | `SYSTEM_ADMIN_PLATFORM_ADMIN_REQUIRED` | 系统管理员未勾选平台管理员 |
| 400 | `INVALID_DEPARTMENT` | 组织角色与部门不匹配 |
| 401 | `UNAUTHENTICATED` | 未登录或 Token 过期 |
| 401 | `INVALID_CREDENTIALS` | 账密错误 |
| 403 | `USER_DISABLED` | 账号已禁用 |
| 403 | `PLATFORM_ADMIN_REQUIRED` | 非平台管理员访问用户管理 |
| 403 | `FORBIDDEN_OPERATION` | 无权限执行当前操作 |
| 403 | `DAILY_REPORT_WRITER_REQUIRED` | 非员工角色写日报 |
| 403 | `WEEKLY_REPORT_WRITER_REQUIRED` | 非员工/中心负责人写周报 |
| 403 | `CENTER_SCOPE_FORBIDDEN` | 跨中心操作被拒 |
| 404 | `USER_NOT_FOUND` | 用户不存在 |
| 404 | `PROJECT_NOT_FOUND` | 项目不存在 |
| 409 | `LAST_ENABLED_SYSTEM_ADMIN_REQUIRED` | 保底约束触发 |
| 500 | `ATTACHMENT_FILE_MISSING` | 附件文件丢失 |
