## 1. Planning

- [x] 1.1 确认新建项目默认初始化为 v20260629 71 项资料模板的目标口径。
- [x] 1.2 确认旧项目继续保持 20260625 64 项资料，不迁移、不补初始化、不改写既有资料记录。
- [x] 1.3 确认 `3.3 合同审核记录表（销售合同）`、`5.4 采购合同审核记录表`、`LC33`、`LC54` 只作为旧项目兼容卡片，不进入 71 项模板。
- [x] 1.4 确认非立项阶段默认文件上传，`1.1 / 1.2 / 1.3` 继续沿用现有在线表单和 `1.2` 专用评价审批。
- [x] 1.5 完成规划文档、proposal、design 和 delta specs，固化不改数据库、不处理文件平台、不删除兼容资料区的实施边界。

## 2. Implementation

- [x] 2.1 找出现有新项目创建链路中的阶段资料模板初始化入口和默认模板版本来源。
- [x] 2.2 实现前固化 v20260629 71 项字段映射清单，至少包括 documentCode、documentName、stageOrder、stageKey、documentOrder、completionMode、submitMode、isRequired，并明确是否使用 targetOutputCode 作为 documentCode。
- [x] 2.3 将新项目默认模板版本从 20260625 64 项切换为 v20260629 71 项。
- [x] 2.4 让新项目初始化使用 `V20260629_TARGET_TEMPLATE_OUTPUTS` 或等价稳定 71 项模板来源。
- [x] 2.5 确保 `LC33 / LC54` 和旧合同审核资料不参与新项目初始化、不计入 71 项模板数量。
- [x] 2.6 保持旧项目按已有项目级阶段资料记录运行，不迁移、不补初始化。
- [x] 2.7 保持 `1.1 / 1.2 / 1.3` 在线表单、`1.2` 专用评价审批、精准返工和前置门禁不回退。
- [x] 2.8 确保非立项阶段资料默认使用现有文件上传或附件上传能力，不新增流程引擎或第二套状态机。
- [x] 2.9 调整兼容资料区标题、摘要和提示为模板版本中性文案，不固定写“当前 64 项资料已迁移”等旧项目专属说明。
- [x] 2.10 调整必要的 smoke 或校验脚本，覆盖新项目 71 项、旧项目 64 项、`LC33 / LC54` 不进入新项目。
- [x] 2.11 保持兼容资料区可访问，不删除、不隐藏、不物理移除组件。
- [x] 2.12 README 运行口径已从 20260625 / 64 项更新为 v20260629 / 71 项。

## 3. Verification

- [x] 3.1 验证新建项目默认生成 v20260629 71 项资料，并带有稳定模板版本标识。
- [x] 3.2 验证新建项目不包含 `3.3`、`5.4`、`LC33` 或 `LC54` 项目资料记录。
- [x] 3.3 验证旧项目继续返回 20260625 64 项资料，不发生迁移、补初始化、状态改写、责任人改写或附件改写。
- [x] 3.4 验证阶段推进、工作台待办、项目工作区和兼容资料区均按项目实际资料集合判断。
- [x] 3.5 验证 `1.1 / 1.2 / 1.3` 在线表单和 `1.2` 专用评价审批未回退为普通上传或普通提交审核。
- [x] 3.6 验证非立项阶段资料可继续通过现有文件上传或附件上传能力处理。
- [x] 3.7 验证兼容资料区标题、摘要和提示适配 20260625 旧项目与 v20260629 新项目，不固定显示“当前 64 项资料已迁移”等错误说明。
- [x] 3.8 验证兼容资料区仍保留且未删除、未隐藏。
- [x] 3.9 运行 API check。
- [x] 3.10 如修改前端源码，运行 Web build。
- [x] 3.11 运行 OpenSpec change 严格校验。
- [x] 3.12 运行 OpenSpec 全量严格校验。
- [x] 3.13 验证 README 不再把 20260625 / 64 项描述为当前新项目默认模板。

## 4. Future Boundaries

- [x] 4.1 记录：旧项目迁移、旧项目补初始化或已创建项目回滚必须通过后续独立 change 评估，不在本 change 执行。
- [x] 4.2 记录：文件平台联动、文件平台目录创建或文件平台归档恢复必须通过后续独立 change 处理，不在本 change 执行。
- [x] 4.3 记录：非立项阶段在线表单化、付款流、发票流、项目模式分支或 BPM/流程引擎必须通过后续独立 change 处理，不在本 change 执行。
- [x] 4.4 记录：兼容资料区隐藏、删除或物理移除必须通过后续独立 change 处理，不在本 change 执行。
