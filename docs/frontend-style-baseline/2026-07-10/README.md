# 前端统一样式视觉基线（2026-07-10）

## 1. 基线环境

- 浏览器：Microsoft Edge Headless（本机已安装版本）
- 前端地址：本地 Vite 开发服务
- 桌面视口：1280 × 900
- 平板视口：768 × 1024
- 手机视口：375 × 812
- 全局主色：`#3e63dd`
- 认证处理：截图及文档不保存账号、密码、Token 或浏览器会话数据

## 2. 修改前构建基线

| 产物 | 原始大小 | Gzip |
|---|---:|---:|
| CSS | 497.87 kB | 67.62 kB |
| JavaScript | 1,312.51 kB | 412.60 kB |
| HTML | 0.42 kB | 0.31 kB |

Vite 修改前共转换 1673 个模块。构建仅包含已有的 chunk 大小提示和 `@vueuse/core` PURE 注释提示。

## 3. 源码存量

| 项目 | 数量 |
|---|---:|
| `styles.css` | 4221 行 |
| 原生 `<button>` | 约 105 |
| 原生 `<input>` | 约 50 |
| 原生 `<select>` | 约 21 |
| 原生 `<textarea>` | 约 12 |
| `.primary-button` 引用 | 约 65 |
| `.ghost-button` 引用 | 约 86 |

阶段 0/1 不以减少上述存量为目标。控件迁移和旧 CSS 删除在后续阶段执行。

## 4. 截图清单

### 已生成

| 页面 | 路由 | 桌面 | 平板 | 手机 | 结果 |
|---|---|---|---|---|---|
| 登录页 | `/`（未登录状态） | `desktop-1280/login.png` | `tablet-768/login.png` | `mobile-375/login.png` | 通过 |

### 待已有业务会话补录

以下页面需要有效业务账号、对应角色权限和现有项目/报告数据。本次执行环境没有可复用的已登录自动化会话，默认初始化账号密码也已被修改。按照“不写入凭据、不修改数据库、不临时造业务数据”的约束，本轮不伪造截图。

| 页面 | 路由示例 | 所需条件 | 状态 |
|---|---|---|---|
| 项目总览 | `/#/projects` | 可查看业务项目的用户 | 待补录 |
| 我的工作台 | `/#/my-stage-document-tasks` | 存在当前用户待办 | 待补录 |
| 立项节点页 | `/#/projects/{id}/node/{nodeKey}` | 可见项目处于或经过立项阶段 | 待补录 |
| 方案设计节点页 | `/#/projects/{id}/node/{nodeKey}` | 可见项目处于或经过方案设计阶段 | 待补录 |
| 日报列表 | `/#/daily-reports` | 可查看日报 | 待补录 |
| 日报编辑 | `/#/daily-report/{id}` | 可编辑或查看的日报 | 待补录 |
| 周报列表 | `/#/weekly-reports` | 可查看周报 | 待补录 |
| 周报编辑 | `/#/weekly-report/{id}` | 可编辑或查看的周报 | 待补录 |
| 周报审批 | `/#/weekly-report-review/{id}` | 审批权限和待审周报 | 待补录 |
| 用户管理 | `/#/users` | `system_admin + isPlatformAdmin` | 待补录 |

补录时沿用本目录的三个固定视口和文件命名规则，且不得提交认证信息。

## 5. 业务回归清单

### 列表与筛选

- [ ] 项目状态、阶段和关键字筛选参数保持不变
- [ ] 日报、周报筛选和分页结果保持不变
- [ ] 空状态、加载状态和接口错误可正常展示
- [ ] 项目卡片仍能进入当前进行节点

### 保存与提交

- [ ] 日报保存草稿、提交及失败恢复正常
- [ ] 周报保存草稿、提交及失败恢复正常
- [ ] 日期、时间和数字字段 payload 类型保持不变
- [ ] loading 期间不能重复提交

### 审批与退回

- [ ] 立项审批、退回原因和节点刷新正常
- [ ] 周报审批通过、退回和权限控制正常
- [ ] 方案设计各节点审批和退回正常
- [ ] 过程节点的完成/退回整改状态显示正确

### 文件操作

- [ ] 阶段资料附件上传、下载和删除正常
- [ ] 在线表单图片上传、下载和删除正常
- [ ] 方案设计文件上传、替换和下载正常
- [ ] 所有上传继续携带现有 Bearer token

### 导航与刷新

- [ ] 顶部主导航、二级导航和移动端侧栏交互不变
- [ ] 工作台 `targetRoute` 定位正确
- [ ] 节点页 `business-state-changed` 刷新协议不变
- [ ] 阶段自动推进后定位到新的当前阶段

## 6. 阶段 1 视觉检查结果

- 登录页在三个视口下无溢出、裁切或布局跳动。
- Element Plus 主色已桥接到 `#3e63dd`。
- `MainLayout.vue` 未修改，顶部主导航、二级导航和移动端侧栏结构保持原样。
- 旧 `.primary-button`、`.ghost-button` 和原生控件样式未删除。
- 未新增 `.el-button`、`.el-input` 等大范围内部结构覆盖。
- 受保护业务页面的视觉与业务回归需在已有业务会话中按上表继续补录。

### 修改后构建对比

| 产物 | 修改前 | 修改后 | 变化 |
|---|---:|---:|---:|
| CSS | 497.87 kB | 499.44 kB | +1.57 kB |
| CSS Gzip | 67.62 kB | 68.06 kB | +0.44 kB |
| JavaScript | 1,312.51 kB | 1,312.51 kB | 0 |
| JavaScript Gzip | 412.60 kB | 412.60 kB | 0 |

CSS 增量来自设计变量、Element Plus 主题桥接和职责区段注释，未引入新的运行时代码或组件依赖。

## 7. 已知提示

- 生产构建仍提示主 JavaScript chunk 超过 500 kB；本轮不处理按需导入或代码分包。
- Rollup 会移除 `@vueuse/core` 中位置无法识别的 PURE 注释；不影响构建成功。
- 本轮只建立主题变量，不应以 CSS 行数下降作为验收条件。
