# Vue 样式集中治理记录

- 日期：2026-07-13
- 范围：`digital-platform-web/src` 全部 Vue 页面、布局和业务组件。
- 样式入口：`digital-platform-web/src/styles.css`。

## 治理结果

- Vue `<style>` 块：21 个迁移前，0 个迁移后。
- 完全重复 CSS 规则：删除 154 条。
- 同一媒体上下文中的同选择器冲突：从 82 个降为 0。
- 迁入规则按页面或组件根类限定，避免原 scoped 样式全局串扰。
- 裸 `:deep()`、`::v-deep` 和裸 Element Plus 内部控件覆盖：0。
- `.primary-button`、`.ghost-button` 模板引用：0；对应失效 CSS 选择器已删除。
- 项目详情普通操作按钮、责任人选择、文本输入和复选项已迁移为 Element Plus 控件。

## 保留的原生控件

- 文件上传的隐藏 `input[type=file]`：浏览器文件选择所需的底层控件。
- 主导航、移动菜单、流程树节点和 Toast 关闭按钮：承担无表单语义的结构化导航或可访问性交互，保留原生 button。

## 自动验证

- `npm.cmd run build`：通过。
- `npm.cmd run check`：通过。
- `git diff --check`：通过，仅存在工作区既有 LF/CRLF 提示。
- `rg -n "<style" src -g "*.vue"`：无结果。
- CSS 冲突分析：`conflictingSelectors = 0`。

浏览器多页面和 1280/768/375px 视觉回归仍应在本地服务运行时执行；本记录不将未实际执行的视觉检查记为通过。
