## 1. 后端阶段推进基础

- [x] 1.1 确认 `project_stages` 已包含 `started_at` 和 `completed_at`，如目标环境缺失则新增迁移补齐
- [x] 1.2 在项目阶段仓储或服务中增加查询项目当前阶段的能力，并能识别没有当前阶段或多个当前阶段的异常状态
- [x] 1.3 在项目阶段仓储或服务中增加按 `stage_order` 查询下一阶段的能力
- [x] 1.4 新增 `POST /api/projects/:projectId/stages/advance` 路由
- [x] 1.5 为阶段推进路由接入 `requireAuth`
- [x] 1.6 确认阶段推进接口不接收、不信任目标阶段、目标阶段顺序或目标阶段标识

## 2. 后端阶段状态机与事务

- [x] 2.1 校验项目存在，不存在时返回项目不存在错误
- [x] 2.2 校验只要项目 `status = completed` 就拒绝推进，即使异常数据中仍存在当前阶段也不修改数据
- [x] 2.3 校验项目有且只有一个当前阶段，异常时拒绝推进且不修改数据
- [x] 2.4 校验当前阶段 `stage_status` 必须为 `current`，异常时拒绝推进且不修改数据
- [x] 2.5 校验非第 8 阶段必须存在下一顺序阶段，且下一阶段顺序为 `current.stage_order + 1`
- [x] 2.6 校验非第 8 阶段的下一阶段 `stage_status` 必须为 `not_started`，否则拒绝推进且不修改数据
- [x] 2.7 校验非第 8 阶段的下一阶段 `is_current` 必须为 `0`，否则拒绝推进且不修改数据
- [x] 2.8 在数据库事务中完成当前阶段 `completed`、`is_current = 0` 和 `completed_at` 更新
- [x] 2.9 在数据库事务中完成下一阶段 `current`、`is_current = 1` 和 `started_at` 更新
- [x] 2.10 实现第 8 阶段 `closeout` 推进后项目 `status = completed` 且不再有当前阶段
- [x] 2.11 确认阶段推进失败时事务回滚，不改变项目状态或任何阶段状态
- [x] 2.12 确认阶段推进只做 `requireAuth`，不实现项目经理角色校验、复杂权限、角色权限或轻角色校验

## 3. 后端齐套门禁

- [x] 3.1 复用或抽取阶段适用必填资料齐套摘要计算逻辑，供阶段推进检查当前阶段使用
- [x] 3.2 确认门禁只统计 `is_required = true` 且适用的资料项
- [x] 3.3 确认 `confirmed` 计为完成，`not_submitted`、`submitted`、`returned` 计为未完成
- [x] 3.4 确认建议资料和不适用资料不影响门禁计数和缺失列表
- [x] 3.5 确认 `requiredTotal = 0` 时允许推进
- [x] 3.6 确认 `incompleteRequiredCount = 0` 时允许推进
- [x] 3.7 确认 `incompleteRequiredCount > 0` 时拒绝推进且不修改项目或阶段状态
- [x] 3.8 阶段推进被齐套门禁拒绝时返回可读错误和缺失适用必填资料列表
- [x] 3.9 确认缺失资料列表每项至少包含 `id`、`documentCode`、`documentName` 和 `status`

## 4. 后端响应与边界

- [x] 4.1 阶段推进成功响应返回更新后的项目基础状态或足够前端刷新所需的结果
- [x] 4.2 为阶段推进相关错误补充稳定错误码和中文错误信息
- [x] 4.3 确认 `status = completed` 的项目继续调用阶段推进时被拒绝，即使异常残留当前阶段也不得推进
- [x] 4.4 确认阶段推进不调用文件管理平台、不创建文件或表单记录
- [x] 4.5 确认阶段推进不创建业务日志、阶段推进操作人完整日志、通知、个人待办、责任人分配或管理层看板指标

## 5. 前端项目详情页

- [x] 5.1 在项目 API 客户端中封装 `POST /api/projects/:projectId/stages/advance`
- [x] 5.2 在项目详情页当前阶段区域展示“手工推进阶段”入口和说明
- [x] 5.3 页面文案明确阶段推进基于当前手工状态和人工适用性判断，不代表文件已上传或已归档
- [x] 5.4 根据当前阶段 `completenessSummary` 展示适用必填资料总数、已确认数、未完成数和完成百分比
- [x] 5.5 当前阶段存在缺失适用必填资料时展示缺失列表和不能推进提示
- [x] 5.6 当前阶段未齐套时阻止或禁用推进操作
- [x] 5.7 当前阶段齐套时允许已登录用户触发手工阶段推进
- [x] 5.8 阶段推进失败时展示可读错误，并在接口返回缺失列表时展示缺失适用必填资料
- [x] 5.9 阶段推进成功后刷新项目详情、8 阶段状态和阶段资料清单
- [x] 5.10 第 8 阶段推进成功后展示项目已完成或当前阶段为空的合理状态
- [x] 5.11 确认页面不提供目标阶段选择、跳阶段、回退、批量推进、审批流、权限配置、文件上传、文件下载、在线表单、业务日志、通知或个人待办入口

## 6. README 与文档

- [x] 6.1 更新 `digital-platform-api/README.md`，说明阶段推进接口、`requireAuth`、齐套门禁口径、第 8 阶段完成项目口径和明确不做范围
- [x] 6.2 更新 `digital-platform-web/README.md`，说明项目详情页手工阶段推进、缺失适用必填资料展示、成功刷新行为和明确不做范围
- [x] 6.3 确认 README 文案明确齐套门禁基于当前手工状态和人工适用性判断，不代表文件已上传或已归档

## 7. 验证

- [x] 7.1 验证未登录调用阶段推进接口返回 401 或需要登录错误
- [x] 7.2 验证当前阶段缺失适用必填资料时阶段推进被拒绝，并返回缺失资料列表
- [x] 7.3 验证当前阶段适用必填资料全部 `confirmed` 时可推进到下一阶段
- [x] 7.4 验证当前阶段 `requiredTotal = 0` 时允许推进
- [x] 7.5 验证第 8 阶段推进后项目 `status = completed` 且没有当前阶段
- [x] 7.6 验证 `status = completed` 的项目再次推进被拒绝
- [x] 7.7 验证 `status = completed` 但异常残留当前阶段时仍拒绝推进且不修改数据
- [x] 7.8 验证非第 8 阶段下一阶段不存在、顺序异常、`stage_status` 不是 `not_started` 或 `is_current` 不是 `0` 时拒绝推进且不修改数据
- [x] 7.9 验证前端推进成功后刷新项目详情、8 阶段状态和阶段资料清单
- [x] 7.10 运行后端基础检查或等价测试
- [x] 7.11 运行前端构建或等价检查
- [x] 7.12 运行 `cmd /c openspec validate add-project-stage-advance --strict`

## 8. 归档前清单初始化门禁修复

- [x] 8.1 更新 `design.md` 和 `specs/project-core/spec.md`，明确当前阶段资料清单未初始化时必须拒绝推进
- [x] 8.2 更新后端阶段推进门禁，当前阶段没有任何 `project_stage_documents` 资料行时返回稳定错误码且不修改项目或阶段状态
- [x] 8.3 更新前端错误码中文提示，说明当前阶段资料清单尚未初始化时不能推进
- [x] 8.4 更新 README，说明阶段推进要求当前阶段资料清单已初始化
- [x] 8.5 验证当前阶段资料项为空时推进被拒绝且项目/阶段状态不变
- [x] 8.6 验证当前阶段资料项存在但适用必填资料数为 0 时仍允许推进，并重新运行后端检查、前端构建和 OpenSpec 校验
