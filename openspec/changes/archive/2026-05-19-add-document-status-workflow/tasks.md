## 1. 后端数据模型与状态机

- [x] 1.1 新增数据库迁移，为 `project_stage_documents` 补充 `returned_by_user_id` 和 `return_reason` 字段
- [x] 1.2 更新阶段资料清单数据表初始化逻辑，保证新环境也包含退回追溯字段
- [x] 1.3 更新资料项映射逻辑，返回 `submittedByUserId`、`submittedAt`、`confirmedByUserId`、`confirmedAt`、`returnedByUserId`、`returnedAt` 和 `returnReason`
- [x] 1.4 实现资料项状态机领域校验，只允许 `not_submitted` / `returned` -> `submitted`、`submitted` -> `confirmed`、`submitted` -> `returned`
- [x] 1.5 为退回操作增加非空退回原因校验

## 2. 后端状态操作接口

- [x] 2.1 新增标记提交接口，要求登录态并记录提交人和提交时间
- [x] 2.2 新增确认接口，要求登录态并记录确认人和确认时间
- [x] 2.3 新增退回接口，要求登录态并记录退回人、退回时间和退回原因
- [x] 2.4 确认状态操作接口校验资料项属于 URL 中的项目
- [x] 2.5 确认已退回资料项重新标记提交时清空本次退回原因、退回人和退回时间
- [x] 2.6 确认非法状态流转返回可读错误，且不改变资料项状态或追溯字段
- [x] 2.7 确认状态操作只使用 `requireAuth`，不实现复杂权限、角色权限或轻角色校验
- [x] 2.8 确认状态操作不调用文件管理平台、不创建文件记录、不创建表单记录、不计算齐套率、不写业务日志

## 3. 前端项目详情页

- [x] 3.1 扩展项目 API 客户端，封装标记提交、确认和退回接口
- [x] 3.2 在阶段资料清单区域增加“手工标记状态”说明文案
- [x] 3.3 为 `not_submitted` 和 `returned` 状态资料项提供“标记提交”操作
- [x] 3.4 为 `submitted` 状态资料项提供“确认”操作
- [x] 3.5 为 `submitted` 状态资料项提供退回原因输入和“退回”操作
- [x] 3.6 在前端校验退回原因非空，未填写时不调用退回接口
- [x] 3.7 操作成功后刷新该项目阶段资料清单
- [x] 3.8 操作失败时展示可读错误提示，并保留当前页面上下文
- [x] 3.9 展示提交、确认和退回追溯字段，允许追溯字段为空
- [x] 3.10 确认已确认资料项不提供本变更范围内的重新提交、退回或取消确认操作

## 4. 验证与边界检查

- [x] 4.1 验证 `not_submitted` 资料项可以手工标记为 `submitted`
- [x] 4.2 验证 `returned` 资料项可以重新标记为 `submitted`，并清空退回追溯字段
- [x] 4.3 验证 `submitted` 资料项可以确认成 `confirmed`
- [x] 4.4 验证 `submitted` 资料项填写原因后可以退回成 `returned`
- [x] 4.5 验证退回原因为空时后端和前端都拒绝退回
- [x] 4.6 验证非法状态流转被后端拒绝
- [x] 4.7 验证未登录或登录态失效时不能执行资料项状态操作
- [x] 4.8 验证项目详情页展示手工状态说明，并且没有文件上传/下载、在线表单、齐套率、阶段推进、业务日志、责任人分配、个人待办或权限配置入口
- [x] 4.9 运行后端基础检查和前端构建或等价检查
- [x] 4.10 运行 `cmd /c openspec validate add-document-status-workflow --strict`
