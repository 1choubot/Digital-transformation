## 1. Planning

- [x] 1.1 创建 `migrate-stage-document-common-actions-to-workspace-cards-v1` change scaffold。
- [x] 1.2 撰写 proposal，明确本阶段迁移旧资料清单通用操作，不切换 71 模板、不迁移旧项目、不新增复杂流程引擎。
- [x] 1.3 撰写 design，固化 Phase 1 / Phase 2 / Phase 3 路线和第一版实现边界。
- [x] 1.4 撰写 project-core-frontend、stage-document-checklist、project-core、technical-architecture 增量 specs。
- [x] 1.5 新增 `docs/9.23_旧资料清单通用操作迁移到项目工作区产出卡片规划_20260702.md`。

## 2. Implementation - 产出卡片通用操作迁移

- [x] 2.1 梳理旧资料清单当前通用操作、后端接口、权限字段、状态字段和组件入口。
- [x] 2.2 将项目工作区产出卡片绑定现有资料的 documentId 或稳定 documentCode，未绑定资料只展示 shell 占位。
- [x] 2.3 在产出卡片展示责任人，并复用现有责任人分配/清空能力。
- [x] 2.4 在产出卡片展示附件列表，并复用现有附件上传、下载、删除能力。
- [x] 2.5 在产出卡片迁移提交资料、审核通过、审核退回入口，并复用现有权限和状态流转。
- [x] 2.6 在产出卡片迁移返工重提/返工完成入口，并复用现有返工字段和完成规则。
- [x] 2.7 在产出卡片迁移标记不适用、恢复适用入口，并复用现有适用性规则。
- [x] 2.8 在产出卡片展示阻塞原因、权限原因和状态说明。
- [x] 2.9 保护 `1.1 / 1.2 / 1.3` 立项在线表单和 `1.2` 专用评价审批，不新增普通上传或普通提交绕过入口。
- [x] 2.10 确认产出卡片没有创建第二套上传、提交、审核、退回规则，没有把 71 项候选落库。

## 3. Implementation - 下方旧资料清单降级

- [x] 3.1 在迁移前保持下方旧资料清单可访问和可操作。
- [x] 3.2 在迁移中将上方产出卡片表达为主入口，并在下方旧资料清单显示辅助/兼容提示。
- [x] 3.3 对已迁移资料，下方旧清单移除主操作按钮，只保留状态、责任人只读、附件摘要、阻塞原因、兼容提示和定位到上方产出卡片的入口。
- [x] 3.4 确认上方产出卡片和下方旧资料清单读取同一套后端资料状态、权限、责任人、附件和阻塞原因。
- [x] 3.5 建立旧资料清单隐藏/删除前的覆盖验收清单，但本 change 不隐藏、不物理删除旧清单组件。

## 4. Verification

- [x] 4.1 在 `digital-platform-api` 运行 `cmd /c npm.cmd run check`。
- [x] 4.2 在 `digital-platform-web` 运行 `cmd /c npm.cmd run build`。
- [x] 4.3 在仓库根目录运行 `cmd /c openspec validate migrate-stage-document-common-actions-to-workspace-cards-v1 --strict`。
- [x] 4.4 在仓库根目录运行 `cmd /c openspec validate --all --strict`。
- [x] 4.5 浏览器/人工验收上方产出卡片可完成现有资料通用操作。
- [x] 4.6 浏览器/人工验收下方旧资料清单不再作为主入口。
- [x] 4.7 浏览器/人工验收 `1.1 / 1.2 / 1.3` 立项在线表单和 `1.2` 专用评价审批不回退。
- [x] 4.8 验收 v20260629 71 项模板未切换为新项目默认模板。
- [x] 4.9 验收旧项目未迁移、未补初始化、未改写 64 项资料状态。
- [x] 4.10 验收桌面和移动 viewport 下产出卡片、操作按钮、附件列表、状态说明、旧清单兼容区不重叠、不溢出。
- [x] 4.11 验收操作仍使用同一套后端状态、权限、业务日志和阶段推进判断。
- [x] 4.12 验收未新增数据库 migration，未创建第二套状态机或流程引擎。
- [x] 4.13 验收已迁移资料在下方旧清单不再出现责任人分配、上传、提交、审核、退回、返工、不适用等主操作按钮。
- [x] 4.14 验收本 change 未隐藏、未物理删除旧资料清单组件。

## 5. Future Boundaries

- [x] 5.1 记录：隐藏或删除下方旧资料清单必须通过后续独立 change 评估，不在本 change 执行。
- [x] 5.2 记录：v20260629 71 项模板默认启用必须通过后续独立 change 评估，不在本 change 执行。
- [x] 5.3 记录：后续 71 项模板切换必须明确模板版本、旧项目迁移策略、回滚策略和验收范围。
